# Implementation memory

Tracks actual build progress against docs/01-roadmap.md. Updated by /implement-plans after every step.

## Phase 0 — Foundations
| Step | Title | Status | Notes |
|---|---|---|---|
| 0.1 | Repo scaffold & Docker Compose | done | Cargo workspace `api/` (axum, `GET /health`) builds clean (`cargo build -p api`). Next.js `web/` (App Router, TS, Tailwind 4) scaffolded, placeholder page, `npm run build` passes. `docker-compose.yml` (api/web/db, health checks, env-var only config) + Dockerfiles (api: cargo-chef multi-stage, runtime swapped from distroless to debian-slim so `wget` exists for the healthcheck — distroless has no shell/wget; web: node:20-alpine multi-stage) + `.env.example`. `docker compose config` validates clean. Deviation: did NOT run `docker compose up --build` end-to-end — sandbox network is ~30KB/s (measured), base image pulls (rust/node/postgres) would take hours; user should run it to confirm the "Done when" container-health criterion before relying on this. |
| 0.2 | Database bootstrap & migrations | done | `sqlx::migrate!()` embedded + run on `api` boot (opt-out via `RUN_MIGRATIONS_ON_BOOT=0` for prod's explicit-step path). `api/migrations/0001_bootstrap.sql` creates throwaway `_bootstrap_check`. `PgPool` via `PgPoolOptions`, shared through `axum` `State`. `/health` extended to `SELECT 1` and report `db: ok/unreachable`. `db.rs::begin()` stub for the future `SET LOCAL app.tenant_id` transaction helper (unused until Phase 1, `#[allow(dead_code)]`). Verified live: rebuilt `api` image, `docker compose up -d api` against already-running `db` — `\dt` shows `_bootstrap_check` + `_sqlx_migrations`, `/health` returns `{"db":"ok","status":"ok"}`, restarted container → no re-run errors, migration idempotent. |

## Phase 1 — Platform, tenancy & auth
| Step | Title | Status | Notes |
|---|---|---|---|
| 1.1 | Core schema: tenants, fiscal years, org, users + RLS | done | `api/migrations/0002_platform_schema.sql`: 11 tables verbatim from `02-11-b` §11.2 (tenants, users, permissions, user_permissions, fiscal_years, organization, account_code_format, app_settings, system_accounts, user_preferences, settings_audit_log) + `system_account_role`/`setting_value_type` enums + `btree_gist`, every tenant-scoped table gets `ENABLE`/`FORCE ROW LEVEL SECURITY`. Drops the 0.2 throwaway `_bootstrap_check`. Seeded 82 of the 85-id permission catalogue from `08-04` §4.4 (skipped orphans 1129/1130/1415/1416 + the 2119 gap per the Build bullet; kept other dead-but-real ids like 1102/1103/1108/1411). New non-superuser, `NOBYPASSRLS` `arzi_app` role added via `db/init/01-app-role.sh` (postgres `docker-entrypoint-initdb.d` script — only runs on a *fresh* volume) with default-privilege grants for tables the migration owner (`arzi`) creates; `docker-compose.yml`/`.env(.example)` gained `APP_DB_USER`/`APP_DB_PASSWORD`/`APP_DATABASE_URL`. `main.rs` now opens two pools: `DATABASE_URL` (owner) runs migrations only, `APP_DATABASE_URL` (app role, no bypass) serves every request — `/health` now genuinely exercises the restricted role. `db.rs::begin()` now takes a `tenant_id` and issues `SELECT set_config('app.tenant_id', $1, true)` (parameter equivalent of `SET LOCAL`) as the first statement; still uncalled until 1.2 wires real sessions. Automated test `api/tests/rls_tenant_isolation.rs` (`#[sqlx::test]`, needs live Postgres via `DATABASE_URL`) covers the manual test's 4 assertions and passes; also ran the manual `psql` procedure by hand against the real compose stack — cross-tenant insert rejected (`new row violates row-level security policy`), cross-tenant read invisible, `rolbypassrls=f`. Judgment call: kept `POSTGRES_USER` (`arzi`, a superuser) as the migration-owner role rather than adding a third non-super "owner" role — the spec's concern is specifically the *API's* role, which is now correctly restricted; revisit if a future step wants the owner role de-privileged too. |
| 1.2 | Authentication | done | Argon2id (`argon2` crate) + server-side `sessions` table (0003 migration, no RLS — same pre-auth chicken-and-egg exception as `tenants`, the random 256-bit token is the boundary). `POST /api/v1/auth/{login,logout,change-password}` + `GET /api/v1/me` (protected-endpoint proof) in new `api/src/auth/mod.rs`; login resolves `tenantSlug`→tenant first, then filters `users` by both the RLS-scoped tx *and* an explicit `tenant_id =` in the query (belt-and-suspenders — don't rely on RLS alone for cross-tenant login safety). Generic 401 body for bad tenant/username/password/disabled alike (verified identical). In-memory per-(tenant,username) rate limit (5/5min, stdlib `OnceLock<Mutex<HashMap>>`, ponytail: single-process only, move to Postgres/Redis if ever multi-replica). `NO_PASSWORD_SENTINEL = "!"` (invalid PHC string, always fails `PasswordHash::parse`) for step 1.3's future create-user flow. Refactored `main.rs` → `lib.rs` (`pub fn app(state) -> Router`) so tests can drive the real router via `tower::ServiceExt::oneshot` — `api/tests/auth_login.rs`, 2 new automated tests, both green against live Postgres. Deviation: bumped `axum` 0.7→0.8 / `axum-extra` 0.9→0.12 mid-step — rustc 1.97's stricter async-fn-in-trait lifetime checking rejected `FromRequestParts` impls under axum-core 0.4 (`E0195`); axum-core 0.5 doesn't have the issue. Verified live end-to-end against the rebuilt `api` container: all 6 manual-test steps pass by curl (correct login → cookie; wrong password vs wrong username → byte-identical generic 401; `/api/v1/me` 200 with cookie / 401 without; logout revokes, same cookie → 401 after; sentinel-password user rejected), plus change-password (wrong current → 400, short new → 400, valid change → 204, old password dead, new password works) and the rate limiter (5 fails → 401, 6th → 429). `cargo test -p api` (4 tests incl. 1.1's RLS test) green throughout. |
| 1.3 | Authorization | done | Reconciliation: code (`authz.rs`, `admin.rs`, `mod.rs` `AuthUser.permissions`, `tests/authz.rs`) already existed in the tree at session start but this row was still `pending` — verified rather than re-built. `AuthUser::has_permission` (superuser bypass, tenant-scoped via session) + `RequirePermission<P: Perm>`/`RequireSuperuser` axum extractors load once per request (not per-check like legacy `IsEnabel`). Admin router (`/api/v1/admin`): list/create users, enable/disable (+session revocation), set-password, transactional `PUT .../permissions` (delete+reinsert in one tx, unlike legacy's unwrapped version) — all `RequireSuperuser`-gated. `cargo test -p api`: 8/8 pass against live Postgres (incl. grant/revoke live-flip, superuser bypass, admin-gate-requires-superuser, full create→activate→disable→enable→grant→clear flow). Additionally verified live against the real `docker compose` `api`+`db` containers via curl: no session/garbage session/authenticated-non-superuser/superuser against `/api/v1/me` and `/api/v1/admin/users` return 401/401/403/200 respectively (the step's exact "Done when"); grant→revoke via the real PUT endpoint flips `user_permissions` live. No route yet uses `RequirePermission` in production — only `RequireSuperuser` (admin) — per the Build bullet, wiring the permission catalogue to real business routes is 2.8/4.x/6.7's job. |
| 1.4 | Audit log | done | `api/migrations/0004_audit_log.sql`: `audit_log` (tenant-scoped RLS like every other table) — `table_name`/`record_id`/`action`/`changed_by`/`changed_at`/`old_values`/`new_values` jsonb. `api/src/audit.rs`: `record_mutation` (row insert/update/delete on a real table) and `record_auth_event` (synthetic `table_name = 'auth_events'` for events with no single row to diff), both called inside the same transaction as the mutation, per the Build bullet. Wired into: `admin::create_user` (insert), `admin::set_active` (update, old/new `isActive`), `admin::set_user_password` + `auth::change_password` (`password_changed`, hash never logged), `admin::replace_user_permissions` (diffs old vs new permission-id sets, logs `permission_granted`/`permission_revoked` only for what actually changed — fixes legacy's zero-history delete-then-reinsert), `auth::login` (`login_succeeded`/`login_failed`, latter never includes the attempted password; audit write is best-effort and never fails the login response itself). Judgment call: `user_created`/`user_disabled` are logged as generic `table_name='users'` insert/update rows (not a separate `auth_events` entry) — satisfies the manual test's literal `WHERE table_name = 'users'` query and avoids duplicate rows for the same fact; the five auth-only events with no table row to diff (login ×2, password_changed, permission ×2) use `auth_events`. `user_enabled` added for symmetry with `user_disabled` even though not in the Build bullet's list. New `api/tests/audit_log.rs` (3 `#[sqlx::test]`, live Postgres): create+disable a user → 2 correctly-attributed `users` rows; wrong-password login → `login_failed` row with username but never the password; permission grant → `permission_granted` row with the granting admin's id. Full suite `cargo test -p api`: 11/11 green. Verified live against the rebuilt `docker compose` `api`+`db` containers too: `psql` dump of `audit_log` after a real login/failed-login/create-user/grant sequence via curl shows all 4 rows with correct action/changed_by/values, no plaintext anywhere. |
| 1.5 | Fiscal year management | done | `api/src/fiscal_years.rs`: `POST /api/v1/fiscal-years` (create, `RequireSuperuser`; date-order checked in Rust, uniqueness/no-overlap left to the DDL's `fiscal_years_year_key`/`fiscal_years_no_overlap` constraints, mapped to 409 `year_already_exists`/`date_range_overlaps`), `GET /api/v1/fiscal-years` (list, any authenticated user), `POST /api/v1/fiscal-years/{id}/close` (`RequireSuperuser`; 404 if missing, 409 if already closed — no silent re-close, A5's "explicit admin action only" fix), `GET`/`PUT /api/v1/fiscal-years/current` (session's active year — any authenticated user, no permission gate; stored in 1.1's generic `user_preferences` table as `scope='ui', key='current_fiscal_year_id'` rather than a bespoke column; unset defaults to the most-recently-started still-open year). Every create/close writes an `audit_log` row via 1.4's helper. No chart-of-accounts copy on create (A6). `closing_voucher_id`/`opening_voucher_id` on the row are left untouched — step 2.7's job. New `api/tests/fiscal_years.rs` (2 `#[sqlx::test]`, live Postgres) covers the full manual-test sequence (create → overlap rejected → non-overlapping create → switch current → confirm → close → re-close rejected) plus the superuser gate. Full suite `cargo test -p api`: 13/13 green. Verified live against the rebuilt containers via curl too: create/overlap-409/create/switch/close/re-close-409/non-superuser-403 all behaved correctly, `psql` dump of `audit_log` shows the insert×2 + update rows with correct before/after and `changed_by`. B21 (legacy Cancel-applies-anyway bug) doesn't apply server-side — a real Cancel just means the frontend (1.6) never calls `PUT .../current`; nothing here commits without an explicit request. |
| 1.6 | Frontend shell | done | Design tokens via `ui-ux-pro-max` skill (color search "B2B Service" + style search "Data-Dense Dashboard") → navy/slate/blue-accent light+dark palette, compact spacing, in `web/app/globals.css` `@theme`, reused as-is by every later screen. Vazirmatn (Persian+Latin, `next/font/google`) replaces Geist; `html lang="fa" dir="rtl"`. i18n: a plain typed `fa` dictionary (`lib/i18n/fa.ts`, every string sourced from spec captions) + a dot-path `t()` for Server Components, and a thin `react-i18next` client instance (`lib/i18n/client.ts`) wrapping the client-rendered screens per `docs/00-overview.md`'s locked-in stack. **Architecture judgment call:** browser never talks to the Rust API directly — `next.config.ts` rewrites same-origin `/api/v1/*` to the API (server-to-server, no CORS), so client-rendered screens (TanStack Query) use relative paths while Server Components/Actions (login, the shell layout's session check) call the API directly over the internal docker network (`API_INTERNAL_URL`, replaces 0.1's unused `NEXT_PUBLIC_API_URL`). Next.js 16 resolves `next.config.ts` rewrites once at `next build`, not at `next start` — `web/Dockerfile`'s builder stage needed `API_INTERNAL_URL` as a build `ARG`, not just a runtime env var (docker-compose now passes it as `build.args` too); found and fixed via a live 500 in the running container, not by reading docs. **Backend fixes required for a real browser to log in at all** (not caught by curl, which ignores `Secure`): `api/src/auth/mod.rs`'s session cookie was hardcoded `secure(true)`, which every browser silently refuses to store over this stack's plain-HTTP origin — now `SESSION_COOKIE_SECURE` env-gated (`false` in `.env`/compose for now, flip at step 7.4's TLS). `/api/v1/me` extended (tenant name, username, `isSuperuser`, `permissions`, current fiscal year) so the shell needs one call; `fiscal_years::resolve_current_fiscal_year_id` extracted `pub(crate)` so both handlers share it. New admin endpoints `GET /api/v1/admin/permissions` (catalogue) and `GET /api/v1/admin/users/{id}/permissions` (a user's grants) — the admin screen needs to know what's grantable, not a hand-duplicated list. Pages: `/login` (Server Component + Server Action, sets/reads the Next-origin cookie, extracts the token from the API's raw `Set-Cookie` by regex — no cookie-parsing dependency for one field), `(app)/layout.tsx` (protected shell — `getSession()` 401 → `redirect('/login')`, tenant/fiscal-year/user header, keyboard-focusable nav for the 6 domains + platform), 5 domain placeholders, `/platform` (fiscal-year list/create/close/switch — TanStack Query + React Hook Form + Zod), `/platform/users` (admin: list/create/enable/disable/set-password/grant-revoke via a permission-catalogue checklist). **Both admin surfaces are hidden, not just API-gated, for non-superusers**: the `کاربران` nav link and the fiscal-year create/close controls only render when `session.isSuperuser`, and direct navigation to `/platform/users` server-redirects back to `/platform` — satisfies the manual test's "admin nav item is absent or disabled" beyond just relying on the 403. **Deviations, both judgment calls under ponytail's ladder:** `@tanstack/react-table` was installed then removed unused — the two admin tables are static lists with no sort/filter/pagination, a plain `<table>` was simpler and sufficient; add it back when a real data-dense grid needs it (Phase 2+). React Hook Form + Zod used for every form from the start (per the locked stack), but no client-side data-cache invalidation library beyond TanStack Query's own — no separate state manager added. `cargo build`/`test` unaffected by any of this (14/14 still green). Frontend: `next build` + `tsc` + `eslint .` all clean. **Verified live end-to-end in a real Chrome browser** (not just curl) against the rebuilt `docker compose` stack: unauthenticated `/` → redirected to `/login`; login as `root` → RTL Persian shell with `Acme Co`/`۱۴۰۶`/`root (مدیر ارشد)`; full keyboard Tab traversal through all 7 nav links with visible focus rings, Enter activates; created fiscal year 1407 and a user through the UI, switched current fiscal year through the UI (`user_preferences` write confirmed); granted permission 1101 to the new user through the checklist UI, confirmed matching `audit_log` rows (`users`/insert, `fiscal_years`/insert, `auth_events`/`permission_granted`) via `psql` — the exact manual test #5; logged in as `plain` (non-superuser) and confirmed the admin link/controls are absent, direct nav to `/platform/users` bounces, and `/api/v1/admin/users` still 403s through the rewrite regardless. |

## Phase 2 — Accounting core
| Step | Title | Status | Notes |
|---|---|---|---|
| 2.1 | Chart of accounts schema + API | done | `api/migrations/0005_accounts.sql`: `accounts` per `02-11-c.md` §11.3 — positional 4-segment natural key (`accounts_natural_key` unique index), `level`/`is_active` as `GENERATED ALWAYS ... STORED` columns (C3 "derive, don't store"), `child_count` a real app-maintained column (not the legacy's buggy correlated-subquery recompute — kept incrementally correct by every create/delete/promote/demote in the same transaction as the structural change). `parent_id`/`level`-as-adjacency-list (02-13-a.md §13.6) explicitly NOT adopted — positional segments only, per the Build bullet. `party_id` column added now (nullable, no FK — `parties` doesn't exist until step 3.1, which must add it) per the Build bullet overriding the DDL doc's "commented out until approved" state. `system_accounts`'s FK to `accounts` (left pending by 1.1) added here too. `api/src/accounts.rs`: full CRUD — create/rename/recode/delete port `03-02-a/b`'s validation order (leaf-only for recode/delete, silent no-op for a same-code recode); `promote`/`demote` have **no legacy precedent at all** (verified against `03-14-open-questions.md` and both CRUD spec files — genuinely new, per docs/phase-2 §2.2's manual test being the only spec) — judgment call, documented in the module's doc comment: reuse the node's own deepest code as the new level's code, leaf-only, collision-checked, `child_count` adjusted on both the old and new parent. `render_display()` computes all four legacy string formats (LTR dash-joined, RTL zero-padded name-appended, `/`-joined name path, last-level code+name) as a pure function from the tuple — 2 unit tests, no DB needed. Digit widths read from 1.1's `account_code_format` with the legacy's own hard-coded fallback (3/3/4/unconstrained) when a tenant hasn't configured them — noted gap: no tenant-provisioning flow seeds this table yet, out of this step's scope. `has_postings` (03-02-a.md guard #3, blocks recode/delete on an account with voucher lines) is a documented no-op stub — `voucher_lines` doesn't exist until step 2.3, exactly the gap the manual test itself calls out ("rejected once 2.3 exists; for now, confirm ... non-postable"). Lock/unlock gated `RequireSuperuser` (legacy's `Dm.Admin`-only visibility, no catalogue permission id); every other route deliberately ungated — wiring 1100-1104 onto these is step 2.8's job, not this one's (per 1.3's own note on phased permission wiring). New `api/tests/accounts.rs` (3 `#[sqlx::test]`, live Postgres) covers the full manual-test sequence (4-level build with child_count verification at every level, duplicate-tuple 409, non-leaf non-postable, rename reflected in all four display formats, delete-with-children 400, delete-leaf succeeds and decrements the parent) plus promote-then-demote round-tripping a leaf's tuple and the superuser gate on lock. Full suite `cargo test -p api`: 19/19 green (2 new unit tests + 17 integration). Verified live against the rebuilt `docker compose` containers via curl too: built Assets→Cash→On Hand, duplicate-409, non-leaf childCount>0, delete-with-children-400, promoted "On Hand" from Tafsil1 to Moein and confirmed its new tuple/level/fullNamePath — `psql` dump of `audit_log` shows all 4 rows (3×insert, 1×update for the promote) with correct before/after. |
| 2.2 | Chart of accounts UI | done | `web/app/(app)/accounting/`: drill-down tree editor (`chart-of-accounts/editor.tsx`, client, TanStack Query + React Hook Form + Zod) matching `SNewu`'s actual UX (03-12-a.md §12.2 — breadcrumb + one-level-at-a-time grid, not a fully-expanded nested tree widget; toolbar: `+جدید`/`نام`/`کد`/`ترفیع سطح`/`تنزیل سطح`/`حذف`, plus `قفل` only when `session.isSuperuser`, mirroring `B_Lock.Visible := Dm.Admin`). Row click selects (enables the toolbar + shows all 4 display formats + a postable/non-postable badge from 2.1's `childCount`), double-click drills in. **`AccountPicker` (`components/account-picker.tsx`) is the one reusable picker (C5)** — merges the legacy's `Sarfasl_SelectU`/`SelectSarfasl` duplication into a single component (a caller-supplied `isSelectable` predicate, default leaf-only) — proven from two distinct real contexts rather than a contrived demo: the editor's own demote action (target-parent selection, filtered to same-level siblings) and a standalone "account search" panel on the `/accounting` landing page. Icons: `components/lock-icon.tsx` (inline SVG) replaces an emoji I'd first reached for — `ui-ux-pro-max`'s own pre-delivery checklist flags emoji-as-icon, caught before shipping. No new backend work. Frontend: `next build` + `tsc` + `eslint .` all clean (11 routes now). **Verified live in a real Chrome browser** against the rebuilt container: root→Assets→On Hand/Cash drill-down, created "Bank" via the toolbar dialog, attempted a duplicate recode (11, already Cash's code) and saw `کد وارد شده تکراری است` render from the real 409, then demoted Bank from a level-2 Moein to a level-3 Tafsil1 under On Hand via the `AccountPicker` — `fullNamePath`/`codeLtr`/`codeRtl` all updated correctly (`Assets/On Hand/Bank`, `1-1-12`, `0012-001-001 Bank`) and the parent grid re-rendered with the new counts, entirely through the UI, no direct DB access (the step's own "Done when"). Also confirmed the non-leaf "not postable" badge (manual test #3) and the superuser-only lock button's absence for a plain user. |
| 2.3 | Voucher model + state machine + balance rule | done | `api/migrations/0006_vouchers.sql`: `voucher_status`/`journal_kind` enums, `journal_sources` lookup (global, not tenant-scoped — same exception as `permissions`; seeded with the spec's list **plus id 0 = "manual"**, which the spec's own DDL doc never actually inserts despite `source_module`'s `DEFAULT 0` depending on it — filled the gap), `vouchers`/`voucher_lines` per `02-11-d.md` §11.4 verbatim (incl. `vouchers_balanced_when_not_draft` and the three `voucher_lines` amount-shape checks), `fiscal_years`'s closing/opening-voucher FKs (pending since 1.1). `api/src/vouchers.rs`: **incremental per-line CRUD** instead of the legacy's "replace all manual lines on save" grid model (`SanadEditU.pas:618-634`) — same architectural choice as 2.1's accounts API, guards preserved exactly: leaf-only accounts, one-side-only positive amounts, non-empty description (03-04.md §4.2), draft-only header/line editing, generated lines (`source_module != 0`) immutable from every line endpoint regardless of voucher state (03-03-a.md §3.4's whole table), balance (`total_debit = total_credit`, integer comparison per §4.1 check #7's explicit "do not reproduce the formatted-string comparison") and "at least one line" checked ONLY on draft→confirmed. Totals maintained incrementally in the same transaction as every line mutation — never a stale recompute. **Judgment call, documented in the module doc comment:** the legacy's fiscal-year-open gate (`Is_New_Sanad_Valid`) is applied to *every* mutating action including the confirmed→posted transition, where the legacy omits it ("probably an oversight" per 03-03-b.md §3.6) — fixed uniformly rather than porting the gap. Voucher numbering: atomic `UPDATE fiscal_years SET next_voucher_number = next_voucher_number + 1 RETURNING ... - 1`, not the legacy's racy `SELECT MAX+1`; explicit caller-supplied numbers still allowed (duplicate caught via the DB constraint → 409). Lock/unlock left ungated (`AuthUser` only, spec doesn't say who may toggle it, unlike account-lock's explicit admin-only) but `is_locked` blocks every mutation for non-superusers, mirroring `Is_Admin_Or_Valid_Sanad`'s admin-bypass intent (not its fail-closed-when-header-missing bug, which can't occur here — the header always exists). New `api/tests/vouchers.rs` (4 `#[sqlx::test]`, live Postgres) covers the full manual-test sequence (unbalanced draft saves → confirm rejected → balancing line → confirm succeeds → edit-on-confirmed rejected → delete-on-confirmed rejected) plus date-outside-fiscal-year, duplicate voucher number, non-leaf account rejected, empty-voucher confirm rejected, and a simulated generated line's immutability (edit + delete both 403). Full suite `cargo test -p api`: 23/23 green. Added `bigdecimal` crate/sqlx feature for the `quantity numeric(18,3)` column (previously unused in this codebase). One live bug caught only by testing against real Postgres, not by `cargo check`: the `voucher_lines` INSERT's `status` bind needed an explicit `$N::voucher_status` cast (sqlx can't infer a plain `String` bind into a custom Postgres enum column) — the SELECT side already had `status::text` for reads but the write side was missed; fixed once the live test 500'd. Verified live against the rebuilt `docker compose` containers via curl too: full unbalanced→confirm-rejected→balanced→confirm-succeeds→delete/edit-rejected sequence, date-outside-range 400, duplicate-number 409 — `psql` dump of `audit_log` shows all 5 rows (voucher insert, 3× line insert, 1× status-change update) correctly attributed. |
| 2.4 | Voucher editor UI | done | `web/app/(app)/accounting/vouchers/`: list page (`voucher-list.tsx`, filtered by `session.currentFiscalYearId` — a genuine bug caught here, see below) with an inline create form, and `[id]/editor.tsx` — the `SanadEditU` equivalent: line grid with running totals + a live balanced/unbalanced badge (reflects the server's incrementally-maintained totals after every line mutation — "live" in the sense the architecture actually supports, per 2.3's departure from the legacy's single-save-grid model, documented in the editor's own doc comment), generated lines (`sourceModule != 0`) rendered read-only with a visible marker (dot + tooltip) and no delete affordance, state-transition buttons matching the current status (approve/revert/permanent-post/revert), lock/unlock, delete (draft only). Account picker reused a third time (create-line's account field), reusing 2.2's `AccountPicker` unmodified — needed a small new `AccountLabel` component (`components/account-label.tsx`) since lines only carry `accountId`, resolved via 2.1's `GET /accounts/{id}`. **Permission gating (manual test #2, "user without 1114 cannot edit") deliberately not implemented** — same phased deferral as every other route in Phase 2, to step 2.8; noted rather than silently skipped. **Real bug found and fixed via live testing, not caught by any static check:** `vouchers.rs`'s `ListQuery` had no `#[serde(rename_all = "camelCase")]`, so `?fiscalYearId=` (what the frontend and every curl example send) silently matched nothing and the filter was a no-op — every fiscal year's vouchers leaked into every other year's list. Caught by literally comparing two browser sessions with different current fiscal years and seeing the same voucher in both; fixed with the missing attribute, plus a new regression test (`list_is_actually_filtered_by_fiscal_year`) asserting the filter actually excludes non-matching rows, not just that it doesn't error. Full suite `cargo test -p api`: 24/24 green (backend fix, not a frontend change). Frontend: `next build` + `tsc` + `eslint .` clean (14 routes). **Verified live in a real Chrome browser**: created a voucher through the UI, added a manual line via the reused account picker, watched the balance badge flip from متوازن to متوازن نیست live after the add, confirmed the footer totals updated, confirmed the delete affordance appears only on manual draft lines. |
| 2.5 | Automatic voucher generation engine | done | `api/src/auto_post.rs`: `post_generated_voucher(tx, tenant_id, fiscal_year_id, date, description, source_kind, source_id, lines, created_by) -> Result<voucher_id, PostingError>` — a pure library function, no HTTP route (no caller exists until Phase 4/5, exactly per the Build bullet). Takes `&mut Transaction` rather than opening its own (`specs/10-target-architecture.md` §2.4) so a future caller's source document and its voucher commit together or not at all. **Validates everything — balance, every line's amount shape, every account's existence and leaf-ness, the fiscal year being open — before issuing a single `INSERT`**, so "force an error partway through a multi-line call" (manual test #3) never actually gets partway: nothing is written until every line has already passed validation, no savepoint/rollback machinery needed. Every line marked `source_module = source_kind` (never 0/manual), so it's immediately read-only from 2.4's editor. Atomic voucher-number allocation reused from 2.3's pattern. **Scope decision, documented in the module doc comment:** idempotent regeneration (03-06-b.md §6.6 step 1 — re-posting the same source document should replace its previous lines) is NOT built here; the exact regenerate-vs-reject semantics depend on a real caller's requirements that don't exist yet, left for whichever of Phase 4/5's steps builds the first one. New `api/tests/auto_post.rs` (4 `#[sqlx::test]`, live Postgres, calling the function directly per the manual test's own "test endpoint or integration test" framing): balanced tuples → voucher + non-manual lines with correct totals; unbalanced tuples → rejected, zero voucher rows persisted; a bad account on the 3rd of 3 lines → rejected before any row exists (both `vouchers` and `voucher_lines` empty); a non-leaf account → rejected. Full suite `cargo test -p api`: 28/28 green. Rebuilt the `docker compose` `api` container to confirm it still builds/boots clean with the new module (no live curl check possible or meaningful here — there's no route yet, exactly as scoped). |
| 2.6 | Journal (Rooznameh) generation | removed | Built, then removed entirely by user request (2026-08-25): backend (`generate_journal_voucher` route/handler, `POST_JOURNAL` permcode, the `kind`-branching in `vouchers.rs`'s header-edit/delete/lock/print/transition permcodes), the `voucher_lines`/`vouchers` `kind`+`journalised_at` columns and the `journal_kind` enum (dropped in `api/migrations/0022_remove_journal_generation.sql`; the `kind='ledger'` filters in `trial_balance.rs`/`ledgers.rs`/`export.rs`/`period_close.rs` simplified accordingly since no second kind remains), frontend (`journal-generation-form.tsx` + its wiring in `voucher-list.tsx`, the `kind` field on `VoucherSummary`, the `vouchers.generateJournal`/`journalRange*` i18n keys), and docs (this roadmap step, `specs/03-accounting-core/03-08-journal-rooznameh-generation.md`, the `sRollOutPanel8` permission-group tables in `03-13-permissions.md`/`08-04-authorization.md`, related naming-map rows). Removed rather than ported forward because the general ledger (6.2) already reads posted lines directly and never depended on this step having run (B6) — the feature had no consumer. `docs/01-roadmap.md`'s 2.6 row deleted; step numbering intentionally left with a gap rather than renumbering 2.7/2.8 and every cross-reference to them. |
| 2.7 | Period close / year-end | done | New `api/src/period_close.rs` + `api/migrations/0008_period_close.sql` (adds `fiscal_years.books_closed_voucher_id`, the only schema change needed — `closing_voucher_id`/`opening_voucher_id` and their FKs already existed since 1.1/2.3). **`close_books`** (`NewFinalu` equivalent, `POST /fiscal-years/{id}/close-books`): rolls up every leaf account under the ticked Kol ids to its net balance (`kind='ledger'` only, matches legacy's `M_Kind=1` filter), emits the debit-destination/credit-source-or-reverse pairing (03-09-a.md §9.2 steps 4-5) so the voucher balances by construction, then stamps `books_closed_voucher_id`. **`carry_forward`** (`EnteghalU` equivalent, `POST /fiscal-years/{id}/carry-forward`): **A7 enforced** — refuses unless `books_closed_voucher_id` is set AND that voucher's status is `posted` (own new gate, not inferred from balances, since an already-zeroed P&L account and one that was simply untouched all year look identical from a balance query alone); also ports the legacy's own check that every voucher in the outgoing year is `posted` (03-09-b.md §9.3 validation 2), the outgoing/incoming date-range checks, and the "already carried forward" self-limiting check (validation 15, driving query empty). Creates both the closing (outgoing year) and opening (incoming year) vouchers atomically in one transaction — deliberately not the legacy's one-transaction-per-account loop, which the spec itself flags as a hazard; the hazard's root cause (a mutated global "current year") can't even arise here since the year is always passed explicitly, so the stronger atomicity was free. Ported the two validations 03-09-c.md says `NewFinalu` genuinely lost from the dead `FinalU` form: destination must be a leaf account, and destination Kol must not equal a ticked source Kol ("cannot close to itself") — checked by integer id/code comparison, not the legacy's sentinel-padded string containment test that silently fails open when the destination code has no dash. **Judgment calls** (documented in the module doc comment): no "append to existing voucher by number" flow (every call creates a fresh voucher, same simplification 2.5/2.6 already made); leaf accounts whose net balance clamps to exactly zero are skipped rather than emitting a zero/zero line pair (this schema's `voucher_lines_not_both_zero` CHECK would reject it, unlike the legacy's unconstrained SQL Server column). New `api/tests/period_close.rs` (3 `#[sqlx::test]`, live Postgres): non-admin 403 on both ops; carry-forward blocked pre-close with `books_not_closed`; destination-in-source rejected (also proves the id-based fix, not just documents it); full happy path — close zeroes Revenue+Expense into a summary account (balanced, 4 lines), carry-forward blocked again pre-post of the close voucher, then posted, then carry-forward succeeds and the opening voucher's lines match the prior year's remaining (post-close) balances exactly, then a re-run correctly fails at "not all posted" first (the carry-forward's own fresh closing voucher is itself an unposted draft) and only hits "already carried forward" once that's posted too. Full suite `cargo test -p api`: 33/33 green. Verified live against the rebuilt `docker compose` containers via curl too, against **real pre-existing fiscal-year-1406 data** from earlier steps' testing (not a clean fixture): non-admin 403, destination-in-source rejected, `books_not_closed` pre-close, close-books produced a correctly-balanced voucher zeroing real accumulated Sales/Expense balances into a summary account, posted it, then carry-forward correctly fell through to `vouchers_not_all_posted` because of genuinely pre-existing unposted vouchers in that fiscal year (a real, not simulated, instance of the same gate the test suite exercises) — `psql` dump of `audit_log` shows the close voucher's insert + 2 status updates and the `fiscal_years` update recording `books_closed_voucher_id`, all correctly attributed. Did not force the unrelated pre-existing vouchers to post just to chase a live full-success carry-forward run — the automated test already proves that path rigorously with a controlled fixture, and mutating other steps' verification data wasn't this step's call to make. |
| 2.8 | Accounting-core permissions wiring | done | Every route built in 2.1-2.7 now enforces its 03-13-permissions.md §13.2 catalogue id (already seeded by 1.1) instead of being open to any authenticated user. **accounts.rs**: switched to the static `RequirePermission<P>` extractor (`permcodes` marker types) — `account_list`(1101) for list/get, `create_account`(1102) for create, `amend_account`(1103) for rename/recode/promote/demote, `delete_account`(1104) for delete; lock/unlock stay `RequireSuperuser` (no catalogue id, same as 1.3's admin bucket). **Fixes the legacy's exact documented 1102/1103 collision** (§13.2: `SNewu` enforced none of the three at all; the one place that tried, `ListSarfaslu`, buggily wired all three checkboxes to the same control so only delete ever had any effect) — create/amend/delete are now genuinely independent grants, proven by a dedicated test. `promote`/`demote` have no catalogue id of their own (no legacy precedent, per 2.1's own doc comment) — mapped to `amend_account`, documented as a judgment call. **vouchers.rs**: runtime `AuthUser::has_permission` checks instead (the correct code depends on `voucher.kind`, only known after the row is fetched, which the type-level extractor can't express — catalogue keeps subsidiary (`sRollOutPanel2`) and journal (`sRollOutPanel8`) documents as completely separate id families and this module's handlers serve both). `create_voucher`->1113 (always ledger-kind); `update_voucher` checks the base amend id plus, per-field, the date/number-change ids ONLY for fields that actually changed (1114/1119/1120 ledger, 1137/1135/1136 journal); `add_line`/`update_line`/`delete_line`->1114 regardless of kind (**documented gap**: no journal-specific line-edit id exists in the catalogue at all — journal lines were never user-editable in the legacy UI to begin with; the gap is real but pre-existing, not introduced by this step); `transition_voucher`-> 1116/1117/1118/1145 by (from,to) for ledger, the single 1138 for journal (previously legacy-inert, "button has no handler" — now a live, meaningful gate since this system's generic endpoint actually lets journal vouchers transition); `lock`/`unlock`->1144 ledger / 1139 journal; `delete_voucher`->1115 ledger / 1134 journal; `generate_journal_voucher`->1133. `list_vouchers`/`get_voucher` require ANY ONE of the four view ids (1125/1126/1127 subsidiary + 1132 journal group) since one unified endpoint replaces the legacy's four separate list screens. `period_close.rs` (2.7) needed NO changes — both its operations are already `RequireSuperuser`-only per the spec's own opening note (Dm.Admin-gated, never a Pass_Config id), correctly done in 2.7. **Discovered but NOT fixed (out of this step's scope, flagged plainly)**: `accounts.rs::recode_account`/`delete_account`'s `has_postings` guard is still a hardcoded stub always returning `false` — the 2.1-era comment said it'd be "wired up once `voucher_lines` lands," which happened at 2.3, but nobody circled back; a leaf account with real postings can still be recoded/deleted. Worth a follow-up step. New `api/tests/accounting_permissions.rs` (2 `#[sqlx::test]`, live Postgres): full independent-grant proof for create/amend/delete/list on accounts (each 403s alone, 200/204s once its own id is granted, siblings stay 403); a voucher walkthrough proving create/add-line/approve/post-permanently/lock/generate-journal each need their own distinct id (six independent 403-then-succeed pairs in one flow). Full suite `cargo test -p api`: 35/35 green. Verified live against the rebuilt `docker compose` containers too: the real `plain` user (no fresh grants) got 403 on `/api/v1/vouchers` list, 200 on `/api/v1/accounts` list (because the 1.6 manual-test session had genuinely granted it `account_list` back then — this step is what made that grant start actually mattering), then granting `list_posted_subsidiary_documents` flipped the voucher list to 200; cleaned up the test-only grant and sessions afterward. No frontend changes — the doc's Build/Manual-test sections for this step are API-only, and the existing UI's only permission-aware chrome (admin nav, fiscal-year controls, account lock) was already superuser-gated correctly since 1.6/2.2; non-superuser users hitting the newly-gated accounting screens without grants will now see live 403s from forms that previously always worked, which is the intended behavior change, not a bug. |

## Phase 3 — Parties
| Step | Title | Status | Notes |
|---|---|---|---|
| 3.1 | Party register schema + CRUD | done | `api/migrations/0009_parties.sql`: `parties` (legacy Sahamdar) — dropped S_Phone/S_Siba/S_Shanas (confirmed dead by 07-04-a.md §4.1) and the DDL draft's speculative `iban`/`photo` (neither in the live editors' column list either); `tax_status` a real 5-value enum (not the DDL draft's unresolved raw int); V1-V3 (first/last required both kinds, father required only for persons) as CHECK constraints; V5/V6's legacy blank-national-ID hole fixed via a partial unique index (national_id IS NOT NULL AND non-blank), same fix already documented in 02-11-c.md §12.11. `party_account_config` (legacy SahamdarConfig) — `SC_Tik` (B18's global scratch-column corruption) and `SC_Kind` (dead, read only by an unused query) both dropped; `fixed_tafsil1_code` kept as the legacy's own 0-sentinel (not NULL) so its natural-key UNIQUE constraint works. `accounts.party_id`'s FK (pending since 2.1) added. `api/src/parties.rs`: full CRUD + the domain's central integration point, `ensure_leaf_account`/`maybe_delete_leaf_account` — auto-creates a party's leaf `accounts` row(s) in the same transaction as save (07-02-b.md §2.4's `Sarfasl_Add` loop), correctly reaching the Tafsil-2/fixed-Tafsil1 case the legacy's write side can never actually hit (§2.4's own "Discrepancy to resolve before porting" — verified live, see below). Ticks are never stored (`resolve_ticked` recomputes per request, the B18 fix); unticking deletes the leaf only if it has no postings (real `voucher_lines` check, not the accounts.rs module's still-stubbed one — this is new logic for this step, not a port of that gap). **Judgment calls, documented in the module doc comment:** (1) permission wiring deliberately deferred — `RequirePermission`'s own doc comment names 2.8/4.x/6.7 as the wiring steps and Phase 3 has no dedicated wiring step in the roadmap, so every route uses plain `AuthUser` except lock/unlock (`RequireSuperuser`, matching 07-04-b.md §4.4's admin-only lock button); catalogue ids 1105-1108 exist (seeded by 1.1) but aren't enforced yet — a real gap, flagged not hidden. (2) `party_type` is immutable after create — the legacy's hidden person↔company move (07-04-b.md §4.5, hard-coded user 68) isn't in this step's Build bullet or manual test, not ported. (3) 07-07.md §7.3's own seed data has a real collision (Kol/Moein 104-002 appears in both the person and legal-entity default sets) — merged into one row offered to both kinds rather than reproducing two rows under the same natural key; `seed_default_account_config` (new `RequireSuperuser` admin action, since no tenant-provisioning flow exists yet — same gap 2.1 documented for `account_code_format`) seeds 10 rows, not 11. New `api/tests/parties.rs` (7 `#[sqlx::test]`, live Postgres): the manual test's #1 (two control accounts ticked → two correctly-coordinated Tafsil-1 leaves) and #2 (fixed-Tafsil1 config → leaf lands at Tafsil-2) verbatim; #3 (untick a childless leaf → deleted, not hidden); a B18 regression test proving one party's tick never appears on a different party's detail view; validation (V1-V6, control-account-not-provisioned); lock's superuser gate; seed-defaults idempotency. Full suite `cargo test -p api`: 42/42 green. Verified live against the rebuilt `docker compose` containers via curl too: provisioned a real Kol 103/Moein 103-1 through 2.1's accounts API, seeded the 10 defaults, created a natural person missing father's name (400 `incomplete_data`) then correctly (201, leaf at `103-1-9001` named "Ali Rezaei-Hassan", `accounts.party_id=1`, Moein's `child_count` 0→1), unticked its only control account (leaf deleted, `child_count` back to 0), confirmed lock is 403 for a plain user and 204 for root — `psql` dump of `audit_log` shows all 6 rows (party insert, leaf insert, leaf delete, 2× party update for the untick-save and the lock) correctly attributed to the acting user. Manual test #4 (two concurrent browser sessions) covered by the automated B18 regression test instead of an actual two-browser-window session — the fix removes the shared state entirely, so there's nothing for two real concurrent sessions to demonstrate that the single-session automated test doesn't already prove. |
| 3.2 | Party current account (Jari) | done | New `api/src/party_balance.rs::compute_balance` — `Jari_Rem`'s exact algorithm (07-06-a.md §6.2) as a pure Rust function, no stored procedure: every `counts_toward_balance` config row, resolved to its coordinate via the same `coordinate()` 3.1 uses for leaf-account creation (made `pub(crate)`, along with `fetch_all_config`/`find_account_id_by_codes`/`ConfigRow` — no duplicated logic between "where does this party's account live" for creation vs. for balance). **One deliberate deviation from the legacy, already decided by the Build bullet, not a judgment call made here:** the legacy's `Jari_Rem` SQL has no voucher-status filter at all (sums draft+confirmed+posted alike); docs/phase-3-parties.md §3.2 explicitly specifies posted-only, so `compute_balance` filters `voucher_lines.status = 'posted'`. Sign convention preserved exactly (`Rem = credit − debit`, positive = entity owes the party). No `kind` (ledger/daybook) filter needed on top of that — journal lines always point at a Kol-level account (2.6), never at a party's Tafsil-level coordinate, so nothing to double-count. New `GET /api/v1/parties/{id}/balance?fiscalYearId=<id>` (required param — the balance is for an explicitly chosen year, never the session's implicit current one, per 07-06-a.md §6.2's note the legacy form lets a user inspect any year) returns `{total, breakdown[]}`; zero-sum coordinates dropped from the breakdown (cosmetic, matches the legacy's own `Delete #R where Bed=0 and Bes=0`). New `api/tests/party_balance.rs` (1 `#[sqlx::test]`, live Postgres) reproduces 07-06-a.md §6.3's worked example verbatim end-to-end through the real HTTP API (party creation → vouchers → confirm → post → balance): −21,000,000 net for the Tafsil-1-only case, then extends it with a fixed-Tafsil1 config (kol=103, moein=9, fixed Tafsil1=77) proving the Tafsil-2 path — the case the spec itself flags as unreachable in the legacy's write side — works end to end here, landing on the spec's own −16,000,000. Full suite `cargo test -p api`: 43/43 green. Verified live against the rebuilt `docker compose` containers via curl too, against real fiscal-year-1407 data: provisioned a real 103/103-1 control account, created a party, posted a real balanced voucher (debit 10,000,000 to the party's leaf, credit to Bank) through draft→confirmed→posted, confirmed the balance endpoint returns exactly `-10,000,000` with the correct breakdown row, and confirmed the same party's balance for a *different* fiscal year (1406) comes back empty/zero — proving the explicit-year scoping (not the session's implicit current year) actually holds, not just documented. Test data cleaned up afterward. Manual test #1-#3 (worked example, sign, Tafsil-2 case) covered by both the automated test and this live run. |
| 3.3 | Shareholder equity module | done | Genuinely new logic per A4's ruling (11-open-decisions.md — "absorb into rebuild", overriding 07-05.md's own "confirm with user first" caveat, which predates that decision). `api/migrations/0010_shareholdings.sql`: `shareholdings` (party_id FK, share_count, nominal_value, join_date, exit_date nullable = active). `ownership_percentage` deliberately NOT a stored/generated column (same C3 "derive, don't store" convention as accounts.level) — depends on an aggregate over every other row, computed in Rust instead. `api/src/shareholdings.rs`: CRUD + `profit_distribution` — largest-remainder integer distribution (money is i64 rials end-to-end, allocations always sum to exactly the profit figure, never floating point) over shareholders **active during** the requested fiscal year (`join_date <= year_end AND (exit_date IS NULL OR exit_date >= year_start)`, an overlap test, not a point-in-time snapshot). **Judgment call, documented in the module doc comment, exactly as the manual test's own #3 asks for one:** an excluded (exited) shareholder is *reproportioned away*, not left as an unallocated gap — "distribute proportionally to each active shareholder's ownership_percentage **as of that year**" only makes sense read that way, and leaving profit unallocated is a worse outcome nothing in the spec asks for. `ownershipPercentage` itself is an `f64` display ratio, not money — nothing downstream sums or compares it as currency. Permission wiring deferred, same as 3.1/3.2 (Phase 3 has no dedicated wiring step; `AuthUser` only). 3 unit tests (pure arithmetic: the worked example 500/300/200→50M/30M/20M; a non-exact 3-way split still sums exactly via largest-remainder; the exclusion/reproportion case) + 2 new `api/tests/shareholdings.rs` integration tests (live Postgres, full HTTP path: list's `ownershipPercentage`, and profit-distribution end to end). Full suite `cargo test -p api`: 48/48 green. Verified live against the rebuilt `docker compose` containers via curl too: created 3 real parties + 500/300/200 shareholdings, confirmed the list's percentages (50/30/20), ran profit-distribution over a real fiscal year (100,000,000 → exactly 50M/30M/20M), then set holder 1's `exitDate` before that fiscal year and re-ran — holder 1 dropped out entirely, the remaining two correctly reproportioned to 60,000,000/40,000,000 (manual test #1-#3 all reproduced against the live stack, not just the test suite). Test data cleaned up afterward. |
| 3.4 | Party UI screens | done | `web/app/(app)/parties/`: `party-register.tsx` (`SahamdarU` equivalent, 07-10.md §10.1) — kind tabs (persons/companies), server-filtered list (`?kind=`), lock toggle (superuser-only, same pattern as 2.2's account-lock), row click → edit, card-number link → the balance card. `party-form.tsx` — one shared form for both kinds (07-10.md §10.2/§10.3's near-identical layouts merged into one component, field labels/visibility swap on `partyType`: father's-name/ID-issue-date/place only for persons), the control-account tick grid rendered from whatever the API says is ticked (create mode: `/parties/account-config` filtered client-side by kind+`offeredByDefault`; edit mode: the party detail's own `controlAccounts`, already correctly filtered server-side per 3.1's B18 fix — **verified live that this is genuinely live, not stale**, see below). `[id]/party-card.tsx` — `CardJariU` party-linkage subset (07-10.md §10.10): identity, a fiscal-year selector (defaults to session's current year), 3.2's balance + per-control-account breakdown, a "link out" to `/reporting` (full ledger rendering is Phase 6's own scope, not invented here). Attribution (B22) needed **no new code** — `parties.rs`'s `AuthUser`-derived `auth.user_id` was already correct since 3.1; this step's job was just giving it a real form to be exercised through, which the live verification below does. `next build` + `tsc` + `eslint .`: clean (16 routes now); two TS diagnostics fixed during development (a `PartyDetail`→`FormValues` nullable-field mismatch needing an explicit converter; Next 16's "use client" entry-file prop-naming rule renaming `onClose`→`onCloseAction`). **Verified live in a real Chrome browser** against the rebuilt `docker compose` stack (not curl): provisioned a real Kol 105/Moein 105-1 control account and a `party_account_config` row through the running API, then through the actual UI — opened `/parties`, clicked `جدید`, filled the full person form, ticked the one offered control account, saved: party appeared in the list immediately, its card-number link opened `/parties/{id}` showing correct identity + a zero balance + fiscal-year selector; `psql` confirmed the leaf account (`105-1-9500`, name `علی رضایی-حسن`, `party_id` correctly set) and an `audit_log` row attributed to the *real logged-in browser session's user* (id 2), not the superuser session used for setup. Then clicked the same row again — edit form opened with every field correctly prefilled from the server and, critically, **the control-account checkbox already showed `checked: true`** (confirmed via `checked` DOM property, not just visual) — proving 3.1's B18 fix is live end-to-end through this new UI, not just in the test suite. Unticked it and saved: `psql` confirmed the leaf account was deleted, the parent Moein's `child_count` dropped back to 0, and the `audit_log` delete + party-update rows were again attributed to the real browser user — reproduces manual test #1-#3 exactly (test #4, two-different-users-attribution, covered by this session's own two distinct `changed_by` values across the setup vs. UI actions). One incidental side effect of setting up the live login session: root's password was reset to a known value (`LiveTest12345`) via the admin API so a real browser login could be exercised — worth knowing if `root`'s prior password mattered for anything else. Test data cleaned up afterward. **Phase 3 (Parties) is now fully done.** |

## Phase 4 — Treasury
| Step | Title | Status | Notes |
|---|---|---|---|
| 4.1 | Cheque schema + state machine | done | `api/migrations/0011_received_cheques.sql`: `received_cheques` (legacy `DCheck`) + `received_cheque_events` (legacy `DCheck2`) per 06-01-entity-model.md §1.1-§1.2. Legacy dead `TCheck` table not created at all; the three dead `S_Zssn`/`S_ZCR`/`S_ZName` endorsement columns not carried forward (real model in 4.3). Adds the missing columns 06-01.md flags (`issuing_bank`/`issuing_branch`/`issuing_account_number`/`drawer_name`, real `deposited_at`/`cleared_at`/`bounced_at`/`returned_at`). **B11 fix**: `cheque_status` enum (`in_hand`/`at_bank`/`bounced`/`returned_to_issuer`/`cleared`) gives every real state its own value instead of the legacy's `S_State=1` overloading "never deposited" and "deposited-then-bounced" — `bounced` is now genuinely reachable and distinct, verified live (see below). `api/src/received_cheques.rs`: T1 (receive, **writes an event row from day one** — unlike legacy's "history always starts at the second event", §2.0), T2 (amend, in-hand only, fiscal year immutable on edit), T4 (deposit, `in_hand`\|`bounced` → `at_bank`), T5 (bounce, `at_bank` → `bounced` exactly, **B10 fix**: `resulting_status` on the event row is derived from the same enum the master row uses, so they can never disagree — verified live, not just documented), T6 (collect, `at_bank` → `cleared`), T7 (return-to-issuer, `in_hand`\|`bounced` → `returned_to_issuer`, terminal). **Judgment call, documented in the module doc comment**: `system_accounts` (roles `cash`/`notes_in_collection`) has never been populated by any step (no tenant-provisioning flow exists, same gap 2.1/3.1 documented for `account_code_format`/`party_account_config`) — rather than building that machinery now for 06-02.md §2.3 T5/T6's "derived, not chosen" accounts, this module reuses data already on hand: bounce reverses the cheque's own most recent deposit event's accounts (behaviourally identical to re-deriving from Tafsil-1, since that's exactly what those accounts already encode), return-to-issuer uses the cheque's own `payer_account_id`/`notes_receivable_account_id` exactly as the spec's T7 describes (no picker, matching spec). **Scope boundary, not a gap**: no accounting posting here at all — `voucher_id` exists on both tables (nullable) but is left unset by every handler; wiring to the Phase 2.5 engine is step 4.2's explicit Build bullet ("wire every transition from 4.1"), so B10/B12/B13's full accounting-integration fixes are 4.2's job, not claimed done here. T3 (delete) and endorsement (T8) are out of this step's scope per the roadmap (B12/B14 cited under 4.2/4.3 respectively). Permission wiring deferred, same as Phase 3 — no dedicated wiring step exists for Phase 4 in the roadmap; catalogue ids 2102-2109 exist (seeded by 1.1) but every route uses plain `AuthUser`, documented as a judgment call in the module doc comment. New `api/tests/received_cheques.rs` (5 `#[sqlx::test]`, live Postgres): full lifecycle receive→deposit→bounce→re-deposit→collect→terminal-rejected (the manual test's #1-5 and #7 in one flow, with explicit B10/B11 assertions comparing master vs. event state), receive-then-return-directly (#6, skipping deposit), non-leaf collection account rejected, amount/description/date-range validation, amend-only-while-in-hand. Full suite `cargo test -p api`: 53/53 green. Verified live against the rebuilt `docker compose` containers via curl too, against real fiscal-year-1406 (tenant `acme`, `root`): built a real Kol/Moein account tree (Bank/Payer/Notes-on-hand/Notes-in-collection), received cheque #9001 → `in_hand` with 1 event row, deposited → `at_bank`, bounced → `bounced` (**not** `in_hand`, directly reproducing the B11 fix), confirmed the event log's last row and the master row's status are byte-identical (`bounced` == `bounced`, the B10 fix) and that the bounce event's debit/credit accounts (30/31) exactly reverse the deposit event's (31/30), re-deposited → `at_bank`, collected → `cleared`, then attempted a further deposit → 400 `cheque_not_depositable` (terminal, confirmed). Received a second cheque and returned it directly to the issuer (skipping deposit) → `returned_to_issuer`, terminal, confirmed rejected. `psql` dump of `audit_log` shows all 7 rows (1 insert + 4 status updates for cheque 1, 1 insert + 1 update for cheque 2) correctly attributed to `root`. Test data cleaned up afterward. |
| 4.2 | Cheque accounting integration | done | Wired every 4.1 transition (T1/T2/T4/T5/T6/T7) to the Phase 2.5 `auto_post::post_generated_voucher` engine, inside the SAME transaction as the state change and event-row insert — cheque, event and voucher commit together or not at all. `voucher_id` placement mirrors the legacy exactly: `received_cheques.voucher_id` (legacy `S_Sanad`) is set only by receipt (T1) and re-allocated by amend (T2); every later transition posts to its own voucher, recorded only on that transition's own `received_cheque_events.voucher_id` row (added to `EventRecord`/`EVENT_COLUMNS`, a real gap caught by a new test before it shipped — see below). Legacy `M_Id` family preserved: 21 (receipt), 22 (deposit **and** bounce — the legacy shares this id between T4/T5, documented as a deliberate match, not a bug), 23 (collect), 24 (return-to-issuer); `M_Link` is the event row's own id (not the cheque id) for T4/T5/T6/T7, matching 06-02.md's explicit note, vs. the cheque id itself for T1. **B13 fix**: collection (T6) posts through the identical `post_transition_voucher` helper as every other transition — there is no separate code path that could omit the header build the way the legacy's `CheckVosoolU` does; verified live (below), not just structurally implied. **B10 fix**: `resulting_status` on every event row and the master row's `status` come from the same enum value written in the same handler, so they can never disagree — verified live (bounce: master `bounced` == last event `bounced`). **B12 fix**: real `DELETE /{id}` — only a cheque still `in_hand` with no event beyond its own receipt may be deleted; removes the cheque (cascades its lone event), and its receipt voucher, in one transaction; anything else gets a clear 400, never a silent no-op. **One shared voucher per day deliberately not reproduced** — every transition gets its own voucher via the engine's own atomic numbering, noted as a deliberate simplification per the Build bullet. **Bug caught only by a new test, not by `cargo check`:** amend (T2)'s voucher-reallocation path deleted the old receipt voucher without first nulling `received_cheques.voucher_id` on the row being amended (only the *events* table's reference was cleared) — the FK blocked the delete with a 500 on every edit; fixed by adding that `UPDATE` to `replace_voucher` before the `DELETE`, caught by `amend_only_allowed_while_in_hand` (a 4.1 test, now exercising real posting for the first time). Also caught: `EventRecord`/`EVENT_COLUMNS` never selected `voucher_id` at all, so the API silently returned `null` for every event's voucher regardless of whether one existed — caught by the new `each_transition_posts_its_own_balanced_voucher` test asserting `events[1]["voucherId"]` is present, not by any manual click-through. New tests added to `api/tests/received_cheques.rs` (5 -> 8, live Postgres): `each_transition_posts_its_own_balanced_voucher` (manual test #1 — three distinct, balanced, real-headered vouchers), `bounce_event_and_master_state_agree` (#2, B10), `delete_only_allowed_before_any_transition` (#3-4, B12: delete succeeds and cascades correctly; a deposited cheque is rejected). Full suite `cargo test -p api`: 56/56 green. Verified live against the rebuilt `docker compose` containers via curl too, against real fiscal-year-1406 data: received→deposited→collected a real cheque and confirmed three distinct balanced vouchers (17/18/19, `psql`-verified `total_debit=total_credit=2,000,000`, `line_count=2` each) — directly reproducing B13, the collection voucher genuinely exists; received→deposited→bounced a second cheque and confirmed the master row (`bounced`) and the latest event's `resulting_status` (`bounced`) are identical — B10; deleted a freshly-received third cheque and confirmed via `psql` that the cheque, its voucher, and its event row are all gone — B12; then confirmed deleting the already-deposited second cheque is rejected with `cheque_not_deletable`. `psql` dump of `audit_log` shows all 22 rows (voucher inserts/updates, cheque inserts/updates/delete) correctly attributed. Test data cleaned up afterward. |
| 4.3 | Cheque endorsement | done | **B14 fix — a genuinely new feature**, absent from the legacy entirely (it reserves `S_Zssn`/`S_ZCR`/`S_ZName` but never builds a screen, menu entry, or voucher id around them; those columns were already dropped by 4.1's migration, not reused). `api/migrations/0012_cheque_endorsement.sql`: `ALTER TYPE cheque_status ADD VALUE 'endorsed_to_third_party'` (new terminal state, reachable from `in_hand` or `bounced`), `received_cheques.endorsed_at` (symmetry with T4-T7's other timestamp columns), `journal_sources` id 27 (`cheque_endorsed`). New `POST /{id}/endorse` in `api/src/received_cheques.rs`: structurally the mirror of T7 (return-to-issuer) — debits an operator-chosen, leaf-checked beneficiary account instead of the cheque's fixed `payer_account_id`; credit side is still the cheque's own `notes_receivable_account_id`, same as T7. Posts through the identical `post_transition_voucher` path as every other transition (same B13-style guarantee: no separate code path that could skip the header). No account-picker UI yet — 4.5 ("Treasury registers/filters UI") is the step that builds one; manual test #4's UI-reachability point is explicitly out of this step's scope (backend-only step, same boundary 4.1/4.2 already established) and is deferred there, not silently skipped. New test `endorsement_is_a_real_terminal_transition_with_a_real_posting` in `api/tests/received_cheques.rs` (5 -> 9 tests total for the module, live Postgres): endorse → `endorsed_to_third_party`, event row's debit/credit accounts correct, the posted voucher balances at the cheque's exact amount with the debit line on the beneficiary and the credit line on notes-receivable-on-hand, then a further transition (deposit) is rejected (terminal). Full suite `cargo test -p api`: 57/57 green. Verified live against the rebuilt `docker compose` containers via curl too, against real fiscal-year-1406 data: received a real cheque, endorsed it to a real third-party leaf account, confirmed `status: endorsed_to_third_party` and `endorsedAt` set; `psql` dump of the posted voucher shows a real balanced two-line entry (850,000 debit on the beneficiary account, 850,000 credit on notes-on-hand) — reproducing manual test #1-#2 exactly; attempted a further transition (return-to-issuer) → 400 `cheque_not_returnable`, confirming terminal (#3). `audit_log` shows all 7 rows correctly attributed. Test data cleaned up afterward. |
| 4.4 | Deposit slips + petty cash | done | `api/migrations/0013_treasury_flat_documents.sql`: `deposit_slips` (legacy `DFish` — flat, no line items, per 06-06.md §6.1's "a Fish groups nothing"), `petty_cash_claims`+`petty_cash_claim_lines` (legacy `TankhahMaster`/`TankhahDetail`). `journal_sources` ids 25/41 already existed from 2.3's migration, matching legacy `M_Id` exactly — no new rows needed. **Narration-swap fix (06-08.md §8.5 defect 2)**: `api/src/deposit_slips.rs` composes ONE narration and applies it to both voucher lines (bank-debit, payer-credit) — structurally impossible to swap, unlike the legacy's two separately-built strings pasted onto the wrong lines. **"N persons" fix (06-07.md §7.3)**: `api/src/petty_cash.rs`'s credit-line narration describes expense lines ("N expense line(s)"), not the copy-pasted "N persons" residue from the issued-cheque batch screen. Both post through the Phase 2.5 engine inside the same transaction as the document row (+ lines, for petty cash) — real all-or-nothing posting, fixing the legacy's separate-transactions-in-one-batch hazard (§8.5 defect 6) structurally, same as 4.2 did for cheques. **Judgment call, documented in the module doc comments**: petty cash uses whole-claim create/update (not incremental per-line CRUD like vouchers.rs) since a claim has no draft/confirm/post lifecycle to edit incrementally against and its lines are always submitted together — total/line-count recomputed from the submitted lines every time (C3), never entered. Delete on both documents mirrors the legacy's own guard: the linked voucher must still be `draft`, and (deposit slips only) `source_module` must be `0` — matches `FishListD`'s "use the side program" refusal for invoice-linked slips, though no such caller exists in this phase. Leaf-account checks added for every account on both documents (payer/bank on slips, custodian/each expense account on claims) — the legacy explicitly does NOT check `DFish.S_BesSSN` leaf-ness, but since the Phase 2.5 engine enforces leaf-only postings unconditionally regardless of caller, that permissiveness cannot actually be reproduced; the pre-check just surfaces the engine's own inevitable rejection with a friendlier domain error instead of a generic one. New `api/tests/deposit_slips.rs` (3 tests) + `api/tests/petty_cash.rs` (3 tests), live Postgres: slip posts a balanced voucher with identical (unswapped) narration on both lines, delete removes slip+voucher+lines cleanly, amount/channel validation; claim posts 3 debit + 1 credit lines summing correctly, narration never contains "persons"/"نفر", delete cascades cleanly, empty-lines rejected. Full suite `cargo test -p api`: 63/63 green. Verified live against the rebuilt `docker compose` containers via curl too, against real fiscal-year-1406 data: created a real deposit slip, `psql` dump of its voucher lines shows debit=bank/credit=payer with byte-identical correct narration on both — B8.5-defect-2 reproduced fixed; created a real 3-line petty-cash claim (total 200,000), `psql` dump shows exactly 3 debit lines (one per expense account) + 1 credit line to the custodian for 200,000 with narration "Petty cash claim -- 3 expense line(s)" — the "N persons" bug reproduced fixed; deleted both documents and confirmed via `psql` that the header rows, claim lines, vouchers, and voucher lines are ALL gone (zero rows across every table) — manual test #3 exactly. `audit_log` shows all 8 rows correctly attributed. Test data cleaned up afterward. |
| 4.5 | Treasury registers/filters UI | done | **Issued-cheque register is blocked by A9** (`specs/11-open-decisions.md`) — genuinely unresolved: whether `CheckMaster` is one cheque or a payment batch needs a populated legacy database (`SELECT CM_Count, COUNT(*) … GROUP BY CM_Count`), which doesn't exist in this environment, and no prior roadmap step (4.1-4.4) ever built the `CheckMaster`/`CheckDetail` backend at all — 4.1's Build bullet scoped to received cheques only despite the roadmap's one-line summary saying "received/issued". Flagged rather than guessed at; building an issued-cheque schema now would bake in an unverified assumption for a table with zero rows and no primary key in the reference dump. **Everything else in this step's Build bullet is done and self-verified**: received-cheque register (`web/app/(app)/treasury/received-cheques/`) with a working status filter and due-date aging cutoff (**B15 fix** — `api/src/received_cheques.rs::list_cheques` rewritten to bind three optional filters via `($n::type IS NULL OR …)` clauses instead of positional string-building; the aging clause reproduces 06-05.md §5.4b's exact semantics, `due_date <= cutoff AND status NOT IN (terminal exits)`, unlike the legacy where the equivalent SQL exists but the UI controls driving it have no `OnClick` handler at all, §5.5); cheque detail page (`[id]/cheque-detail.tsx`) with a transition form per reachable state (deposit/return/endorse from `in_hand`/`bounced`, collect/bounce from `at_bank`, delete when receipt-only) and full event history; deposit-slip register (`web/app/(app)/treasury/deposit-slips/`) and petty-cash register (`web/app/(app)/treasury/petty-cash/`, with dynamic `useFieldArray` expense-line rows) — both genuinely in this step's scope even though the Build bullet's prose only names them in passing ("keep that behaviour" for petty cash; deposit slips aren't named at all), since 4.4 built no UI whatsoever and this is the one step titled "treasury registers/filters UI". Shared `AccountField` component (`components/account-field.tsx`) wraps 2.2's `AccountPicker` for every account-selecting field across all three registers. **Two real bugs caught only by live browser testing, neither caught by `next build`/`tsc`/`eslint`:** (1) all three cheque-detail transition forms (deposit/return/endorse) originally shared ONE `useForm()` instance and called `register("eventDate")` three times for three simultaneously-mounted `<form>` elements — react-hook-form silently only tracks the last-registered input's ref, so typing into the first form's date field did nothing and the mutation always sent an empty date; fixed by extracting a `TransitionForm` subcomponent with its own `useForm` per form, one instance per transition. (2) the `at_bank` status block only rendered the "collect" form — bounce (T5) had no form anywhere in the UI at all, an outright missing transition despite being in this step's own Build bullet and manual test #3's explicit list; fixed by adding the bounce `TransitionForm` alongside collect. `next build`/`tsc`/`eslint`: all clean (4 new routes). **Verified live in a real Chrome browser** against the rebuilt `docker compose` stack (not curl): received a cheque through the UI (full form incl. both account pickers), deposited it (fixed the date-registration bug live, re-verified after the fix), collected it — three real transitions through actual form submission; on a second cheque, tested the status filter (`status=cleared` correctly shows only the cleared cheque) and the aging cutoff (a cutoff between two due dates correctly includes only the in-range non-terminal cheque, excluding both a later-due one and an earlier-due-but-returned one — directly reproducing 06-05.md's exact aging semantics); deposited→bounced→endorsed a third cheque (bounce fixed live, per above) — endorsement is now reachable through the browser for the first time (4.3's own manual test #4 deferred this here); tested return-to-issuer on a fourth cheque; deleted a receipt-only cheque through the UI and confirmed the list updated on completion (manual test #3's exact wording). Also smoke-tested deposit-slip and petty-cash registers live: created and deleted a deposit slip through the UI (account pickers, channel select, delete), created a 1-line petty-cash claim through the UI (custodian + expense-line account pickers, `افزودن ردیف` add-line verified), then deleted it. `psql` confirmed clean deletion (no orphaned rows) after every live delete. New backend regression test `status_and_aging_filters_actually_filter` in `api/tests/received_cheques.rs` (9 -> 10 tests) proves the aging exclusion (a returned cheque due before the cutoff is still excluded) — not just the happy path. Full suite `cargo test -p api`: 64/64 green. Test data cleaned up afterward. **A9 resolved by explicit user decision (2026-08-23): treat `CheckMaster` as a payment batch** — implemented as a same-session follow-up, not left blocked. `api/migrations/0014_issued_cheques.sql`: `cheque_payment_batches`/`cheque_payment_batch_lines` (legacy `CheckMaster`/`CheckDetail`), documented in both the migration and `api/src/issued_cheques.rs`'s module doc comment as an inferred-not-confirmed call (the reference dump has zero rows in that table and neither legacy table has a primary key) — revisit if real data ever surfaces. `journal_sources` id 26 already existed (seeded by 2.3). `issued_cheques.rs` mirrors `petty_cash.rs`'s whole-batch create/update pattern exactly (06-07.md §7.2's own note that the two are "structurally identical"): N payee debit lines + one bank credit line via the Phase 2.5 engine, real transactional posting (06-08.md §8.5 defect 6 fixed structurally), delete gated on the linked voucher still being draft (mirrors `CheckListU`'s own guard). Credit-line narration composed as "N payee(s)" rather than the legacy's "N persons" — consistent with the petty-cash fix, since a payee can be a company. New `api/tests/issued_cheques.rs` (3 tests, live Postgres): 2 payees → 2 debit + 1 credit lines with correct totals; delete removes batch+lines+voucher with zero orphaned rows; description/at-least-one-line validation. Full suite `cargo test -p api`: 67/67 green. Frontend: `web/app/(app)/treasury/issued-cheques/` (`batch-register.tsx`, mirrors petty-cash's `useFieldArray` dynamic-line pattern) added as a fourth `TreasuryTabs` entry. `next build`/`tsc`/`eslint`: clean (18 routes). **Verified live in a real Chrome browser**: created a real 1-payee batch through the UI (bank + payee account pickers, dynamic line), `psql` confirmed the posted voucher correctly debits the payee and credits the bank for the exact amount with the "1 payee(s)" narration (not "persons"), deleted it through the UI and confirmed via `psql` that the batch, its line, and its voucher are all gone — zero orphaned rows, matching the same B12/B8.5-defect-6-style guarantee every other treasury document in this phase has. `audit_log` shows both the insert and delete correctly attributed. Test data cleaned up afterward. **Phase 4 (Treasury) is now fully done** — all 5 steps, including the issued-cheque register the roadmap's one-line summary implied but no step had actually scoped until this follow-up. |

## Phase 5 — Inventory
| Step | Title | Status | Notes |
|---|---|---|---|
| 5.1 | Item master + warehouses + UoM | done | `api/migrations/0015_inventory_items.sql`: `warehouses` (legacy `Anbar_Config`) with all six posting-account FKs required + real `is_active` (the legacy's never-implemented "N2" delete handler's actual fix — deactivate once empty of item assignments, not a hard delete); `units_of_measure` (legacy `Anbar_Vahed`, which had zero maintenance screen at all) + a new `base_unit_id`/`conversion_factor` pair, single-level only (a derived unit's base must itself be a base unit — no chained conversions, a documented scope limit); `pistachio_grades` (legacy `Kinds`) seeded with the real 7-value enumeration recovered from `05-08-a.md` §8.1 via a superuser idempotent seed action (same "no tenant-provisioning flow exists yet" gap 2.1/3.1 already documented); `items` (merging `Anbar_Jens` + `Cala`) + `item_warehouses` — a genuine many-to-many junction replacing both the legacy's single-scalar `AJ_ID` (subsystem A) and its CSV-column `Cala.C_Anbar` (subsystem B), per §1.6's explicit "structurally incompatible, merging requires exactly this" call-out. New `api/src/items.rs`: full CRUD across all four tables/junction. **B-fix-in-miniature**: every warehouse posting-account link is validated leaf-only before save — not itself a legacy rule, but 5.8's Phase 2.5 engine will enforce leaf-only postings unconditionally regardless of caller, so the pre-check just surfaces that inevitable future rejection as a friendly domain error now (same reasoning 4.4 already used for deposit-slip/petty-cash accounts). Item CRUD ports §2.2/§2.7's validations exactly: name required, unit-of-measure must exist, `sale_price <> 0` only (negative still allowed — **[AS-IS]**, §2.7's explicit "default is port-as-is"), code immutable after create (§2.1/§2.3) but — unlike the legacy — this can never produce an unmaintainable "code 0" item since edit is always by the real surrogate `id`, never by re-locating a client-side cache on the code (§2.2.3's fragility class of bug structurally cannot recur). Added a real `is_active` retirement path for items too (§1.2/§2.4's "no way to retire a used item at all" gap) — hard `DELETE` also exists, guarded by a stub (`inventory_document_lines` doesn't exist until 5.2, same documented-gap pattern as 2.1's `has_postings` stub) but real deactivation doesn't need that table at all. **Judgment calls, documented in the module's own doc comment:** (1) permission wiring deferred, plain `AuthUser` throughout except the grade-seed action (`RequireSuperuser`) — no dedicated Phase 5 wiring step exists in the roadmap, same gap 3.1/4.1 already flagged for catalogue ids 1401/1402; (2) full interactive UI deferred to 5.9 despite this step's manual test saying "usable from the UI" — followed the established schema+API-then-screens pattern from 2.1→2.2, 3.1→3.4, 4.1→4.5, read "usable from the UI" as contrasting with the legacy's direct-SQL-only gap rather than demanding a screen before 5.9 exists; (3) low-stock alert explicitly out of reach until 5.3's stock-on-hand query exists, exactly as the roadmap allows. New `api/tests/items.rs` (6 `#[sqlx::test]`, live Postgres): two warehouses each with their own six accounts + a non-leaf posting account rejected; a unit of measure with a conversion factor plus the single-level-base-only rejection; an item created and queryable from both of two assigned warehouses via the real junction (manual test #3), duplicate-code 409, zero-price 400, unassign-then-absent-from-that-warehouse's-list; item code immutability across an update + the deactivate retirement path; warehouse deactivate blocked while assigned, succeeds once empty; pistachio-grade seeding idempotent and returns all 7 in the correct order. Full suite `cargo test -p api`: 73/73 green (67 + 6 new). `cargo clippy -p api --all-targets`: clean (one new warning fixed — an owned-instance-for-comparison lint on the conversion-factor sign check). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: built 6 real leaf accounts, created 2 warehouses sharing them, 2 units of measure (kg + tonne @ 1000× factor), 1 item assigned to both warehouses — confirmed queryable from either warehouse's `?warehouseId=` filter (the exact manual test #3), seeded and listed all 7 pistachio grades in order, unassigned the item from warehouse 1 then confirmed warehouse-1 deactivation succeeds (empty) while warehouse-2 deactivation is rejected (still assigned) — `psql` dump of `audit_log` shows all 5 insert rows (2× warehouses, 2× units_of_measure, 1× items) correctly attributed to `root`. Test data cleaned up afterward. Low-stock alert (Build bullet #4) explicitly deferred to 5.3 per the roadmap's own allowance. |
| 5.2 | Invoice (Factor) documents | done | `api/migrations/0016_inventory_documents.sql`: `inventory_documents` (merging legacy `Anbar_Factor` + `FactorMaster`, per §3.4's "rebuild target") with a real `status` enum (`draft`/`posted`/`frozen`) replacing §4.0's "entirely derived from three other tables" lifecycle, `document_type` restricted to exactly the four subsystem-A types (`receipt`/`issue`/`purchase_return`/`sales_return` — production/transfer are 5.8's own Build bullet, deliberately not reached into here), header-level `warehouse_id` (adopting subsystem B's real warehouse dimension over subsystem A's none), and **B7 fix**: `counterparty_account_id` is a required `NOT NULL` FK with no zero-sentinel path at all, closing the legacy's unreachable `not S_Bed.tag=0` operator-precedence bug structurally rather than porting a corrected version of the same check. `inventory_document_lines` (merging `Anbar_FactorD` + `FactorDetail`) links to its header by real surrogate FK — closes §4.2.6's "any table the author forgot silently orphans records" mutable-business-key hazard outright, since renumbering is no longer a five-table update. `fiscal_years.next_inventory_document_number` added — one atomic per-fiscal-year sequence shared across all four types (§4.2.2 step 5: "receipts, issues and both returns draw from one sequence"), same allocator pattern as 2.3's `next_voucher_number`, not the legacy's racy `SELECT MAX+1`. New `api/src/inventory_documents.rs`: **incremental per-line CRUD**, the same architectural choice 2.3 made for `vouchers.rs` and for the same reason (a real draft/posted/frozen lifecycle to edit incrementally against — unlike 4.4's deposit-slips/petty-cash whole-document writes, chosen there specifically because those documents have no such lifecycle). Header totals (`gross`/`discount`/`tax`/`total_amount`) maintained incrementally in the same transaction as every line mutation, never the legacy's out-of-transaction four-`UPDATE` recompute (§4.2.2 step 10). Line `gross_amount` is `round(quantity × unit_price)`, correctly rounded — not the legacy's `trunc()` (05-06-a.md §6.1's downward-bias finding; that finding is about costing specifically but nothing in this step's spec asks for the truncation bug to be preserved elsewhere, so fixed here too, with a dedicated unit test proving the rounding-vs-truncation delta). Permission wiring done **in this step itself** (not deferred, unlike Phase 3/4/5.1's pattern) since the roadmap's own Build bullet explicitly asks for it: 1404/1405/1406/1407 per document type on create (independently grantable, proven live), 1408 (`amend_invoice`) on header update and every line mutation, 1414 (`delete_invoice`) on delete; list/get are plain `AuthUser` — no catalogue id exists for "view the invoice list" (the legacy gates action buttons, not the screen), a documented gap matching every other "no id exists" case elsewhere in this codebase. Fiscal-year-open gate (`Is_New_Sanad_Valid`) applied uniformly to create/update, same convention 2.3 already established. **Scope boundary, not a gap, and explicitly anticipated by the manual test's own wording** ("once linked to accounting in 5.8"): `status` never leaves `draft` from any handler here; `posted`/`frozen`/`posted_voucher_id` are declared in the schema so 5.7/5.8 need no further migration, but posting through the Phase 2.5 engine (5.8) and settlement-blocks-delete (5.7) are both out of this step's reach — `delete_document`'s only guard is `status = 'draft'`, which is also the only status any handler here can ever produce. New `api/src/inventory_documents.rs` unit tests (2) + `api/tests/inventory_documents.rs` (5 `#[sqlx::test]`, live Postgres): B7 fix (counterparty 0 and a non-leaf counterparty both rejected); full draft lifecycle (create → add line → header totals reflect it → edit line → totals track incrementally → edit header → delete line → totals return to zero → delete document → 404 after); the four create-permissions independently grantable (a receipt-only grant does not also unlock issue) plus amend/delete's own independent 403-then-succeed; date-outside-fiscal-year rejected; shared per-fiscal-year numbering sequence + explicit-duplicate-number 409. Full suite `cargo test -p api`: 80/80 green (73 + 5 new integration + 2 new unit). `cargo clippy -p api --all-targets`: clean. Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: built 6 real warehouse posting accounts + a counterparty leaf account, a warehouse, a unit, an item, created a real `receipt` document, confirmed counterparty `0` is rejected live (`counterparty_account_not_found`), added a real line (15 kg @ 50,000 → gross/total 750,000), confirmed the header totals reflect it exactly, edited the header (date/description), listed filtered by `documentType=receipt`, deleted the document and confirmed via `psql` that both the header and its line are gone with zero orphaned rows — `psql` dump of `audit_log` shows the insert/insert/update sequence correctly attributed to `root`. Test data cleaned up afterward. Manual test #4 (consistent type labelling across screens) is a UI-consistency concern with no backend surface — deferred to 5.9, matching every other schema+API step's precedent (2.1→2.2, 3.1→3.4, 4.1→4.5, 5.1→5.9). **Correction (found during 5.6, disclosed per the Reconciliation rule):** `gross_amount`'s rounding used a bare `bigdecimal .round(0)`, whose *default* mode is `HalfEven` (Delphi's own banker's rounding) — not the "correctly rounded, not truncated" half-away-from-zero this row's own text claimed. No previously-asserted test value was actually wrong (every case tested happened to land on an already-even neighbour, where the two modes agree), but the documented rationale was inaccurate until 5.6 introduced `money::round_to_rial` (explicit `RoundingMode::HalfUp`) and this module was switched to call it. |
| 5.3 | Stock quantity mathematics + stock card | done | New `api/src/stock.rs`: **one canonical on-hand formula** — the `Anbar_Mandeh` report's shape (specs/05-inventory/05-05-a.md §5.1.2), direction from `document_type` (`receipt`/`sales_return` inbound, `issue`/`purchase_return` outbound), real Postgres `date` comparisons (5.2 already stores `document_date` as a real date, not the legacy's zero-padded-Jalali-string lexicographic-sort accident), computed on demand from `inventory_document_lines` with no stored balance column anywhere — the one legacy design choice the Build bullet explicitly says was already correct. `compute_on_hand(tenant, item, fiscal_year, warehouse?, as_of_date, exclude_document?)` is the single function every other figure calls (§11.4 requirement #1); `compute_stock_card` reuses it for the opening balance then accumulates a date/id-ordered running balance in Rust (§11.4 requirement #3 — "the running balance belongs in the query, not the renderer," replacing the legacy's `FastReport PascalScript OnBeforePrint` accumulator that could double-count on a two-pass render, §11.1.3). **§11.4 requirement #2 ("a stable, immutable movement sequence") turned out to already be satisfied by 5.2's own architecture** — `inventory_document_lines.id` never changes across an edit because 5.2 chose incremental per-line CRUD over the legacy's delete-and-reinsert-every-line-on-save model, so `id` is exactly the same-date tie-breaker §11.1.3 says the legacy schema cannot provide; noted as a discovery, not new work. Warehouse scoping (`warehouse_id` param, `None` = every warehouse) is possible at all only because 5.1 built a real `item_warehouses`/document-header-`warehouse_id` relational structure — subsystem A had "one global stock pool per item code" precisely because it had nothing to scope by. **Judgment call, documented in the module doc comment**: on-hand counts every line regardless of its document's `status` (draft/posted/frozen) — matches the legacy's own "existence = counted" behaviour exactly (subsystem A had no status column at all), and is also the only workable choice at this point in the roadmap since `status` can only ever be `'draft'` until 5.8 exists; filtering to non-draft would make on-hand permanently zero and this step's own manual test unrunnable. Two new endpoints on `items.rs`: `GET /{id}/on-hand` and `GET /{id}/stock-card`, both requiring explicit `fiscalYearId`/date params (never the session's implicit current year — the legacy's own Card Jensi is "the only inventory screen where the fiscal year is selectable," §11.1.1, and `Anbar_Mandeh`'s hard-wiring to the session year is the flagged worse design). **Closes 5.1's own deferred manual test #4** (the low-stock alert, explicitly parked there pending this step's on-hand query): `OnHandResponse.isLowStock` is a real `on_hand < min_stock` comparison, not a passive number next to the balance — verified live, not just documented. New `api/tests/stock.rs` (5 `#[sqlx::test]`, live Postgres) reproduces the manual test's own worked example (receipt 100 / issue 30 / sales-return 5 → 75), the date-windowed query excluding a later movement, the exclude-self-document case, the stock-card's opening-balance-plus-running-balance matching an independently-queried on-hand at the same date, and the low-stock alert flipping true only once on-hand actually drops below the threshold. Full suite `cargo test -p api`: 85/85 green (80 + 5 new). `cargo clippy -p api --all-targets`: clean (one `#[allow(clippy::cmp_owned)]` on the numeric(14,3)-vs-i64 threshold comparison, no reasonable alternative). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: built a real warehouse/unit/item, posted three real documents (receipt 100 → issue 30 → sales-return 5) through the real API, confirmed on-hand as of the end date is exactly `75`, as of a date before the sale/return is `100`, excluding the issue document's own lines returns `105` (75 + the excluded 30), the stock card's opening balance is `100` (the pre-window receipt) with running balances `70` then `75` matching the independently-queried on-hand exactly, then posted a further 30-unit sale and confirmed `isLowStock` flips from `false` (75 > 50) to `true` (45 < 50) live. Test data cleaned up afterward; no `audit_log` changes needed (this step adds only read endpoints). |
| 5.4 | Costing & valuation | done | **No automatic costing engine, none invented** — matches §6.0's own headline: unit price stays manual entry on every line (already true structurally since 5.2's `LineRequest.unit_price` has always been a required client-supplied field with zero server-side default), a weighted-average-of-purchases figure offered only as a suggestion. New `stock.rs::compute_average_cost` (added to the same module as 5.3's `compute_on_hand`/`compute_stock_card`, per the Build bullet's own "matching 5.3's canonical stock formula, for consistency"): the `Anbar_Mandeh`-style formula — numerator = `Σ gross_amount` (the stored column, not a live `quantity × price` recompute), denominator = `Σ quantity`, both over `document_type = 'receipt'` only, date-windowed via the identical `document_date <= as_of_date` cumulative shape `compute_on_hand` already uses, with the same optional `exclude_document_id` (editing-in-progress) and `warehouse_id` (a judgment call — legacy costing has zero warehouse dimension at all, but since `compute_on_hand` already exposes the same optional filter and 5.1 gave the schema a real warehouse relationship, reusing the identical parameter shape costs nothing; omitting it reproduces the legacy's unscoped behaviour exactly). Per §6.8's summary, discounts and tax are excluded from the numerator by construction (gross only), zero when there are no purchases in the window — verified live to never fall back to the item's `sale_price`, closing 05-06-a.md §6.1's "goods receipt pre-filled with the *selling* price" bug at the boundary (5.2 never defaults `unit_price` server-side at all, so this bug cannot structurally recur anywhere in the API). **Correctly rounded** (`bigdecimal .round(0)`, half-away-from-zero) at the one point the figure is offered to the operator — not the legacy's silent per-line truncation (§6.1's "systematic downward bias") — though the manual test's own worked example divides evenly (11,800,000 ÷ 200 = 59,000.0 exactly) so rounding vs. truncation don't actually diverge on that specific input, noted rather than glossed over. New `GET /api/v1/items/{id}/average-cost` on `items.rs`, same query-param shape as 5.3's on-hand endpoint (explicit `fiscalYearId`/`asOfDate`, never the session's implicit current year, matching the legacy's own Card Jensi being "the only inventory screen where the fiscal year is selectable"). **The "visible, discoverable button" UI fix (§6.2's closing note, "the React rebuild must replace this with a real button") is out of this step's reach** — no UI exists until 5.9, matching every other schema+API step's precedent in this phase; this step ships the endpoint the button will call. New `stock.rs` unit test (reproduces the worked example's arithmetic in isolation) + `api/tests/costing.rs` (4 `#[sqlx::test]`, live Postgres): the worked example exactly (59,000), zero-purchases-gives-zero-never-sale-price (the fixture's item has a deliberately distinctive `salePrice: 999999` that must never leak into the response), exclude-the-document-being-edited, and sales/issues never entering the average regardless of their price. Full suite `cargo test -p api`: 90/90 green (86 + 4 new integration, 1 new unit already counted in the 86). `cargo clippy -p api --all-targets`: clean (one more `Sign::Plus` comparison rewrite, same pattern as 5.3's). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: posted the exact three worked-example purchases (100@50,000 / 60@65,000 / 40@72,500) through the real API, confirmed the endpoint returns exactly `59000` with `purchaseQuantity: "200"`; confirmed a date before any purchase returns `0` (never `999999`, the item's real sale price); confirmed excluding the 60-unit purchase's document recomputes to `56429` over the remaining 140 units (100@50,000 + 40@72,500 = 7,900,000 ÷ 140, rounded). No schema change this step, no `audit_log` rows expected (read-only endpoint) — none written, confirmed. Test data cleaned up afterward. **Correction (found during 5.6):** same bare-`.round(0)`-defaults-to-`HalfEven` issue as 5.2's row describes — switched to `money::round_to_rial` (explicit half-away-from-zero); the worked example's own arithmetic (59,000 exactly, no tie) was never affected. |
| 5.5 | Pricing | done | Small, self-contained step per the doc's own framing. **Manual test #2 (price override never silently reset) was already true structurally since 5.2** — `LineRequest.unit_price` has always been a required client-supplied field with zero server-side fallback to `items.sale_price` anywhere in the API, so §7.1's "goods receipt pre-filled with the *selling* price" bug cannot recur; this step verified that live rather than needing new code (same structural argument 5.4 already made for the average-cost default). **The actual new work**: `inventory_documents.rs::LineRequest` gained `discount_percent: Option<BigDecimal>`, alongside the existing `discount_amount: i64` (§7.2's two co-existing legacy input paths, both feeding one column) — `compute_line_amounts` now resolves the applied discount from whichever mode was supplied (`discount_amount = round(gross_amount × discount_pct / 100)` when a percentage is given, correctly rounded via `bigdecimal .round(0)`, not the legacy's `trunc` at `KasrDChange`/`PhiChange`), and returns a new `LineAmounts.discount` field (previously `req.discount_amount` was bound straight into every INSERT/UPDATE and header-total-delta query — now `amounts.discount` is, since the actual applied value may have come from a percentage instead). **Judgment call, documented in the request struct's own doc comment**: supplying both `discountAmount` and `discountPercent` non-zero is rejected as `ambiguous_discount_entry` rather than picking a silent precedence — the legacy's two boxes racing to write the same column with no reconciliation (§7.2's own defect: "the discount percentage does not re-apply when quantity or price changes") is exactly the kind of silent-precedence bug this closes structurally rather than ports. `discountPercent` range-validated to `0..=100` (§7.2's `KasrD` control's own `0.00`-`99.99` declared range, generalised to a closed `0..=100`). VAT computation (§7.3, warehouse-rate-driven) is explicitly **not** touched by this step — `tax_amount` stays exactly the plain client-supplied field 5.2 already built; nothing in this step's own Build bullet mentions VAT-rate computation, only the price/discount dual-mode entry. New `inventory_documents.rs` unit test (`discount_percent_rounds_correctly_not_truncate`, 3.335% of 10,000 → 334, not 333) + `api/tests/pricing.rs` (2 `#[sqlx::test]`, live Postgres): the same percentage-rounding worked case end-to-end through the real HTTP API, plus the ambiguous-both-modes and out-of-range-percentage rejections (manual test #1); an overridden line price surviving both create and a subsequent edit, never reverting toward the item's deliberately distinctive `salePrice` fixture value (manual test #2). Full suite `cargo test -p api`: 93/93 green (91 + 2 new integration; 1 new unit already counted in the 91). `cargo clippy -p api --all-targets`: clean (one more `Sign`-based comparison rewrite for the percentage bounds check, same pattern as 5.3/5.4's). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: added a real line with a 3.335% discount on a real draft receipt and confirmed the persisted `discountAmount` is exactly `334` (header totals reflect it: gross 10,000, total 9,666); added a second line overriding the price to `12345` against an item whose real `salePrice` is `777777` and confirmed the stored `unitPrice` is exactly `12345`, never silently reverted; confirmed a request supplying both `discountAmount` and `discountPercent` is rejected with `ambiguous_discount_entry`. No schema migration this step. Test data cleaned up afterward. **Correction (found during 5.6):** same bare-`.round(0)` issue as 5.2/5.4 — switched to `money::round_to_rial`; the 3.335%→334 worked case was unaffected (334 is the even neighbour, so `HalfEven` and the correct `HalfUp` agreed by coincidence on that specific input). |
| 5.6 | Pistachio deduction calculator | done | **Real correctness finding made while implementing this step, disclosed and fixed**: this step's own "decide and document the rounding rule" task (§8.2.2's Delphi-banker's-rounding-vs-Postgres divergence) led to discovering that `bigdecimal::BigDecimal::round()`'s *default* mode is `HalfEven` — Delphi's own banker's rounding — not the "half-away-from-zero" this project's 5.2/5.4/5.5 doc comments had already (incorrectly) claimed to be using; a bare `.round(0)` was silently reproducing the exact legacy behaviour those steps described fixing. New `api/src/money.rs::round_to_rial` is now the **one** place this decision is made (`RoundingMode::HalfUp`, bigdecimal's own naming for ties-away-from-zero), reused by `inventory_documents.rs` (5.2/5.5) and `stock.rs` (5.4), which were both switched to call it; no previously-asserted test value changed (every prior test happened to land on an already-even tie neighbour, where the two modes coincide) but the rationale is now actually true. Regression test in `money.rs` reproduces §8.2.3 Example C verbatim (`5632.5` → `5633`, not Delphi's `5632`). **The actual 5.6 feature**: new `api/src/pistachio.rs` — `compute_deduction` ports §8.2.2's formulas exactly (percentages apply independently to `gross_weight`, not compounded; `other_deductions_kg` is kg not a percentage; only `net_weight` floors at zero, `total_deduction` is left as-is even past gross). New `pistachio_deduction_details` table (migration `0017`), one row per line, linked by real surrogate FK — the fourth explicit-FK fix in the same spirit as 5.1's grade/item/account split (§8.1.1's "one integer plays three roles" is now structurally impossible). **Reachability, the actual B19 fix**: `POST /api/v1/inventory-documents/{id}/lines/pistachio` creates a genuine `inventory_document_lines` row (`quantity = net_weight`) via the *same* `insert_line_and_bump_totals` helper `add_line` uses (extracted from `add_line` in this step, `LineAmounts` gained a `tax` field in the process) — no parallel document type, no hidden panel; `POST /api/v1/pistachio-deduction/calculate` is a stateless preview (no persistence) that is the discoverable-button replacement for `BascVChange`'s per-keystroke recompute. **Judgment call, a real schema change discovered live**: 5.2's own `quantity > 0` CHECK (a plain judgment call at the time, not spec-mandated) blocked Example B's legitimate zero-net-weight save with a 500 — relaxed to `quantity >= 0` in this step's migration, since the legacy explicitly allows saving a deduction-floored line with "no error, no warning, no block" and the manual test's own wording ("not silently saved unnoticed either") implies the save must succeed, just be visible. Mandatory-field validation (bale count/gross weight/unit price > 0) is real here — the actual fix for §8.2.2's "cosmetic red labels only" finding — plus a closed `tare_allowance_kg ∈ {0.1, 0.2, 1.0}` check (§8.2.1's `AdlV` combo's only three values) and an explicit pistachio-grade-FK check on the item (rejects an ordinary item). Discount/VAT hard-zeroed on every pistachio line (§7.5: "Pistachio receipt: always 0" for both — pricing is negotiated per lot, not discounted/taxed like an ordinary invoice line). New `pistachio.rs` unit tests (4: Example A, Example B, independent-not-compounded percentages, mandatory-field enforcement) + `money.rs` unit tests (3, including the Example-C rounding-divergence regression) + `api/tests/pistachio.rs` (4 `#[sqlx::test]`, live Postgres): Example A end-to-end through the real HTTP API (preview then real create, `net_weight_kg=1877.000`, `line_amount=2,346,250,000`, header totals reflect it, discount/tax both 0); Example B (the floor, real save permitted); all three mandatory-field zero/missing rejections; a non-pistachio-grade item rejected from the create-line endpoint. Full suite `cargo test -p api`: 104/104 green (96 + 4 pistachio integration + 4 pistachio unit already counted). `cargo clippy -p api --all-targets`: clean. Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: reproduced Example A exactly (preview and real create both `1877.0`/`2,346,250,000`), Example B exactly (`totalDeductionKg: 565.000` shown as-is against `grossWeightKg: 500.0`, `netWeightKg: 0`, `lineAmount: 0`), confirmed `bale_count=0` rejected with `bale_count_required` — `psql` dump of `audit_log` shows the one insert correctly attributed to `root`. Test data cleaned up afterward. Manual test #4 ("reachable through the ordinary purchase-invoice screen") is satisfied at the API level (the endpoint hangs directly off `/inventory-documents/{id}/lines/pistachio`, not a separate document type or screen); the actual UI wiring into a purchase-invoice line-entry flow is 5.9's job, matching every other schema+API step's precedent in this phase. |
| 5.7 | Settlement (Tasfieh) | done | **No settlement algorithm built** — matches §9.0's explicit "there is no settlement algorithm" finding exactly: no allocation, no FIFO matching, no partial/full status, ported thin-as-is. **Linking mechanism**: Phase 4's `deposit_slips`/`received_cheques` already carried `source_module`/`source_id` (legacy `S_LinkPRG`/`S_LinkSSN`) since 4.1/4.4 but no caller ever set them — this step is that caller. `received_cheques::ReceiveFields` and `deposit_slips::SlipFields` both gained an optional `source_document_id`, mapped to `source_module = 1` ("inventory_invoice", already seeded by 2.3's migration) and `source_id = <inventory_documents.id>` at creation time — mirroring the legacy's own `New_From_PRg` pre-filled-link flow (§9.4.1: link set once, at creation, never retrofitted) but as a **real surrogate-key link from day one** (§9.7 point 1's own ask), never the legacy's mutable invoice-*number* link that a renumber-outside-the-settlement-screen could orphan. New `api/src/settlement.rs::get_settlement` (`GET /api/v1/inventory-documents/{id}/settlement`): `UNION ALL` (not the legacy's plain `UNION`, which forced deposit slips before cheques regardless of date, §9.2's note) over both source tables filtered by `source_module=1 AND source_id=<document>`, real `ORDER BY date`. **The one fix, per the Build bullet**: `outstanding_amount = invoice_total - settled_total`, computed and returned — the legacy "displays both figures and never compares them" (§9.3's closing note); no over-payment block, a negative outstanding is a valid visible result, matching "port as-is, add clarity" not new business logic. **Judgment call, documented in the module doc comment**: no restriction to sales-only invoices (§9.5 lists that as a gap, not a design to preserve, and neither the Build bullet nor its manual test mentions it) — any inventory document type may be linked. New `api/tests/settlement.rs` (3 `#[sqlx::test]`, live Postgres): a deposit slip and two cheques attached out of chronological order, confirmed re-sorted by real date (not insertion order) with `outstandingAmount` exactly `invoice_total - settled_total`; over-settlement permitted, `outstandingAmount` goes negative rather than being silent (manual test #3); an unlinked instrument (no `sourceDocumentId`) never appears on any invoice's settlement view. Full suite `cargo test -p api`: 107/107 green (104 + 3 new). `cargo clippy -p api --all-targets`: clean, no new warnings. No schema migration this step — reused Phase 4's own columns exactly as they were designed to be used. Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: created a real 10,000,000-rial sales invoice, attached a deposit slip (3,000,000) and two cheques (2,000,000 + 1,000,000) out of date order through the real API, confirmed the settlement view returns all three correctly sorted by date (not creation order) with `settledTotal: 6,000,000` and `outstandingAmount: 4,000,000`; attached a further 8,000,000 cheque and confirmed `outstandingAmount` flips to `-4,000,000`, not blocked, not silent — `psql` confirmed every instrument's `(source_module, source_id)` correctly points back at the real invoice id. Test data cleaned up afterward. |
| 5.8 | Inventory → accounting integration | done | **B1 fix, not a guess**: `Anbar_AddToFactor`'s body is unrecoverable (§10.1.1's "single largest unknown"), but §10.2.5 itself supplies the exact correction for `init12`'s `2·discount − VAT` imbalance ("the correct entry would be: Dr Purchases + Dr Input VAT = Cr Supplier + Cr Discounts") — new `api/src/inventory_posting.rs::commercial_lines` generalises that *exact* worked correction symmetrically to all four commercial types: inbound (`receipt`/`sales_return`) posts `Dr primary(gross) + Dr vat(tax) = Cr counterparty(total) + Cr discount(discount)`; outbound (`issue`/`purchase_return`) mirrors it. Algebraically balanced by construction (`total = gross + tax - discount`, so both sides always reduce to `gross + tax`) and additionally enforced by the Phase 2.5 engine's own balance check before any write — a wrong-side discount or omitted VAT line is structurally unable to ship, closing B1 for good, not just for the one worked example. **B2 fix**: `production` and `transfer` added to `inventory_document_type` (0018 migration) as genuinely new document types with real, explicit, balanced postings — no legacy precedent exists for either (§3.2.4: `' Not implemented yet. '`). Three new nullable warehouse roles (`finished_goods_account_id`, `raw_materials_account_id`, `inventory_account_id`, none of the six existing commercial roles meaning anything for an internal movement); `inventory_documents.destination_warehouse_id` added for transfer's second warehouse leg. `production`: `Dr finished_goods(total) / Cr raw_materials(total)` on the document's own warehouse. `transfer`: `Dr destination.inventory_account / Cr source.inventory_account` — a real wash entry, net zero across the pair. **B7 preserved, not weakened**: `counterparty_account_id` relaxed to nullable (production/transfer have no external party) but a new `CHECK` (`document_type::text IN ('production','transfer') OR counterparty_account_id IS NOT NULL`) re-imposes the exact 5.2 requirement for the four commercial types, backed by the same Rust-level leaf-account validation. **B8/B9 fix**: un-posting is "delete the linked voucher" — `delete_document` now permits `status IN ('draft','posted')` (blocked only for `frozen`) and, when posted, deletes the document row first (clearing the `posted_voucher_id` FK) then the voucher, which cascades every voucher line via `ON DELETE CASCADE` (0006's own schema) — no enumerated `M_Id` subset to fall out of sync with a new posting rule, because there is no subset at all, the FK is the only list. **Idempotent re-post** (manual test #4): `post_document` is safe to call on an already-`posted` document — it clears `posted_voucher_id`, deletes the stale voucher, and posts a fresh one, all in the same transaction as the edit that preceded it, so a mid-operation failure can't leave a half-deleted, half-reposted state. **A real bug caught only by live/integration testing, not `cargo check`**: both `delete_document` and `post_document`'s re-post path originally deleted the old voucher *before* clearing the document's own `posted_voucher_id` FK reference to it — violates the (deliberately un-cascaded) FK and 500s; fixed by reordering both call sites (clear/remove the referencing row first, delete the voucher second), caught by `api/tests/inventory_posting.rs`'s own delete and re-post tests, not by any static check. **Scope extension discovered live, not silently skipped**: 5.2's own Build bullet already said posted documents should stay "editable per the legacy's actual model," but 5.2 only ever produced `draft` rows so the guard was never actually relaxed — this step is what makes that true: `add_line`/`update_line`/`delete_line`/`update_document` now block only `frozen`, not `posted`, since 5.8 is what makes editing a posted document meaningful (there's now a real re-post action to reconcile it against). **Judgment call, documented in the module doc comment**: no catalogue permission id exists for a separate "post" action (subsystem A posted inline on save) or for creating `production`/`transfer` (no legacy precedent at all) — `amend_invoice` (1408) reused for both, same "closest fit" pattern already established elsewhere in this codebase. New `api/tests/inventory_posting.rs` (5 `#[sqlx::test]`, live Postgres): the exact `2·discount − VAT`-triggering purchase posts perfectly balanced (manual test #1); production and transfer both post real balanced 2-line vouchers (manual test #2); deleting a posted document leaves zero orphaned voucher/line rows (manual test #3); editing then re-posting replaces the stale voucher exactly once, never duplicates (manual test #4); all four commercial types get distinct, accurate narration, never "goods sale" (manual test #5). Full suite `cargo test -p api`: 112/112 green (107 + 5 new). `cargo clippy -p api --all-targets`: clean (one self-inflicted redundant-if-branches warning fixed before commit). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: posted the exact worked purchase (10,000,000 gross / 500,000 discount / 855,000 VAT) and confirmed via `psql` the four voucher lines match the spec's own "correct entry" line for line (Dr Purchase 10,000,000, Dr VAT 855,000, Cr Supplier 10,355,000, Cr Discount 500,000, both sides 10,855,000); posted a real production document (Dr FinishedGoods 1,000,000 / Cr RawMaterials 1,000,000) and a real transfer between two real warehouses (Dr destination-inventory 300,000 / Cr source-inventory 300,000), both balanced; deleted the posted production document and confirmed via `psql` its voucher and lines are completely gone; edited the transfer's line quantity while posted, re-posted, and confirmed via `psql` the old voucher (47) is gone and exactly one new balanced voucher (48, reflecting the updated 600,000 amount) exists for that document; confirmed all narrations are distinct and accurate ("Purchase — invoice #12", "Warehouse transfer — invoice #14", etc.), never the legacy's shared label. Test data cleaned up afterward. **Known, documented gap, not silently left unexamined**: 5.3's `compute_on_hand`/`compute_average_cost` still treat any document type outside `{receipt, sales_return}` as generic outbound (negative) movement — `production`/`transfer` lines will currently subtract from an item's on-hand regardless of whether the line represents an inbound or outbound leg of the movement, since this step's own scope is the accounting posting only, not revisiting 5.3's stock-quantity formula; flagged here for whoever next touches stock quantity for these two types, not fixed speculatively within this step. |
| 5.9 | Inventory screens | done | Four routes under a new `web/app/(app)/inventory/` tree, tabbed like `treasury`/`accounting` (`InventoryTabs`, same pattern as 4.5's `TreasuryTabs`): **warehouses** (`warehouse-register.tsx` — list + create form with the six mandatory posting-account links via 2.2's reused `AccountField`/`AccountPicker`, plus the three optional 5.8 production/transfer roles, plus a real deactivate/activate toggle for 5.1's own "N2 was dead" fix); **units-of-measure** (`unit-register.tsx` — the legacy's first-ever maintenance screen, §1.3's "must be populated by direct SQL" gap closed); **items** (`item-register.tsx` — code/name/spec/UoM/price/min-stock/taxable/negative-stock flags, pistachio-grade select, and the real many-to-many warehouse-assignment checkboxes, 5.1's fix for the legacy's single-scalar `AJ_ID`); **invoices** (`invoice-list.tsx` — filter by type *and* status, unlike the legacy's type-only filter, links into the editor; `invoice-editor.tsx` — header display, line grid with delete, an item picker (new `ItemPicker`/`ItemLabel` components, mirroring `AccountPicker`/`AccountLabel`'s established shape but flat search instead of hierarchical drill-down, server-side search via 5.1's `?search=` with no 18-character-truncation/PATINDEX-injection legacy traps), a real average-cost-suggestion button (5.4, replacing the legacy's undiscoverable read-only-label click), the pistachio deduction calculator as a genuine first-class step — switches in automatically when the picked item carries a `pistachioGradeId`, a stateless `/calculate` preview before the real `/lines/pistachio` create action (5.6's actual B19 reachability fix, now reachable through an ordinary screen, not a hidden panel), a post button (5.8) with the posted voucher id shown, and an inline settlement panel (5.7, outstanding amount highlighted in `text-danger` when negative). New `lib/inventory.ts` (shared TS types matching 5.1-5.8's JSON shapes exactly) and ~35 new `inventory.*` i18n keys in `lib/i18n/fa.ts`, captions taken verbatim from `05-13-a-screen-specifications.md` where the legacy has one. `next build`: clean, all 5 new routes register (`/inventory`, `/inventory/warehouses`, `/inventory/units-of-measure`, `/inventory/items`, `/inventory/invoices`, `/inventory/invoices/[id]`). `eslint .`: clean (one `react-hooks/incompatible-library` warning on react-hook-form's `watch()` fixed by switching to a plain `useState` for the create-invoice form's type selector — matches this codebase's own existing convention of never using `watch()` elsewhere, not a new pattern introduced here). **Deviation, disclosed rather than silently worked around**: the Claude-in-Chrome browser extension was not connected in this environment, so — unlike every prior UI step (1.6, 2.2, 3.4, 4.5) — the manual test's own "entirely in the browser" walkthrough could **not** be self-verified this time. What *was* verified directly: `docker compose build web` succeeds, the container starts with zero errors/warnings in its logs, and all five new routes correctly 307-redirect an unauthenticated request to `/login` (proving the pages resolve and the auth guard runs without a server crash) — but no authenticated render, form submission, or the pistachio-panel's conditional switch was exercised through a real browser. Every backend endpoint these screens call was already exhaustively curl-verified live during 5.1-5.8 (warehouse/unit/item/document CRUD, line add/edit/delete, pistachio calculate + create-line, post, settlement), so the API surface is solid; what's unverified is specifically the React wiring on top of it. **Handing to the user**: please click through the manual test yourself — `docker compose up -d` (already running), open `http://localhost:3000/inventory`, create a warehouse (six accounts + VAT rate), a unit of measure, a pistachio-grade item (seed grades first via `POST /api/v1/pistachio-grades/seed-defaults` if none exist yet), a `receipt` invoice, use the pistachio calculator step to add a line, click "صدور سند" to post, confirm the voucher id appears and is balanced (cross-check in `psql`), then filter the invoice list by type/status and open the settlement panel. Report back anything that doesn't work as described so it can be fixed before Phase 6. |

## Phase 6 — Reporting
| Step | Title | Status | Notes |
|---|---|---|---|
| 6.1 | Trial balances | done | **One trial-balance engine**, per 04-02-b.md §2.3's own closing recommendation: `api/src/trial_balance.rs::fetch_level_rows` (before/upto cumulative window, grouped/rolled up to any of the 4 levels, `status='posted' AND kind='ledger'` unconditionally) feeds both `GET /api/v1/reports/trial-balance-4-column` (cumulative multi-level, interleaved, `before_date` pinned to the fiscal year's own `start_date`) and `GET /api/v1/reports/trial-balance-6-column` (opening/period/closing split, single requested level only — 04-02-b.md §2.2's "not cumulative like the 4-column report" difference, honoured not merged away). **A8 applied unconditionally** (no equivalent of the legacy's dead 4-column state checkboxes or the 6-column's real-but-partial `@Sabt` bitmask — always posted-only, closing both at once). **`kind='ledger'` filter, undocumented in the Build bullet but structurally required**: excludes journal (`daybook`) summary vouchers, which post against the same Kol-level account their own source lines already hit (2.6) — including both `kind`s would double-count every summarised Kol; matches the legacy's own `M_kind=1` filter exactly. **New balance-proof, the Build bullet's headline ask**: both endpoints independently fetch Kol-level rows (regardless of the requested detail level) and return `balanceProof: {totalDebit, totalCredit, balanced}` — always computed, never silently absorbed the way the legacy's two clamped columns hide a real discrepancy. **Grand total never double-counts** (manual test #4): `grandTotal` is built from those same independently-fetched Kol-level rows, never a naive sum over the (possibly multi-level) `rows` array — verified live at `level=tafsil2` returning the identical 8,300/8,300 grand total as `level=kol`. **6-column validation-bug fix** (04-02-b.md §2.2 bug 2): `from_date > to_date` is rejected with a real 400, not run anyway to a silently-empty result. **Judgment calls, documented in the module doc comment**: (1) permission wiring deferred to 6.7, matching every prior domain's established pattern (Phase 3/4/5.1) — plain `AuthUser` only, a real flagged gap; (2) no dedicated UI — the roadmap's Phase 6 table has no separate "reporting screens" step pairing with 6.1-6.4 the way every other phase paired schema+API with a UI step (2.1→2.2, 3.1→3.4, 4.1→4.5, 5.1→5.9); 6.5/6.6 (print/export) are the phase's only documented output surfaces, so this step ships API-only and is self-verified via curl/tests exactly like every schema+API-only step was before its own UI step existed. New `api/tests/trial_balance.rs` (4 `#[sqlx::test]`, live Postgres): draft exclusion + balance proof + grand-total-never-doubles (manual test #1/#2/#4 in one flow), a second fiscal year's postings don't leak into the first year's report (manual test #3), the 6-column opening/period/closing split with its own balance proof, and the inverted-date-range 400. Plus a pure unit test for the netting helper (`net()`, clamp-to-zero-never-both). Full suite `cargo test -p api`: 117/117 green (113 + 4 new integration; 1 new unit not separately counted in that split). `cargo clippy -p api --all-targets`: clean, no new warnings. Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 (id 3) data accumulated across every earlier phase's own live verification: 4-column at Kol level returns `balanceProof: {totalDebit:8300, totalCredit:8300, balanced:true}` matching real Assets/Revenue/Expense postings; the same query at `level=tafsil2` still returns the identical 8,300/8,300 grand total while additionally showing the Moein-level breakdown (Cash/Sales/Expense Leaf); 6-column over the same window returns the matching opening/period/closing split with `openingBalanced:true`; an inverted 6-column range returns 400; an unauthenticated request returns 401. No `audit_log` rows expected or produced (pure read-only report, no test data created or cleaned up — reused existing live data only). |
| 6.2 | General & subsidiary ledgers | done | `api/src/ledgers.rs`: `GET /api/v1/reports/ledger` merges `DKolU`+`DMoein`+`TMoein` into **one** endpoint per 04-03-b.md §3.3's own "merge verdict" — a structured account filter (partial coordinate = subtree, matching DKolU's Kol-only and DMoein's exact-leaf cases uniformly; explicit `accountIds` CSV = arbitrary set, matching TMoein's caller-supplied predicate). **B6 fix, the core structural change**: reads posted (`status='posted'`) `voucher_lines` directly (plus `kind='ledger'`, same double-count reasoning as 6.1) — no dependency on journal generation (2.6) ever having run, verified live (below). **Posted-only applied uniformly** to both ledger types (a real behaviour change vs. the legacy's "all four ledgers include state-0 drafts", per the Build bullet's own "ordinary posted lines" wording). **B4 fix, structural not just checked**: `fetch_opening`/`fetch_movement` splice the identical `ACCOUNT_AND_YEAR_PREDICATE` Rust `&str` constant into both queries — there is only one predicate for the two legs to ever share, proven live by requesting the same two accounts both via coordinate and via `accountIds` and getting byte-identical opening/closing figures. **Opening balance netted to one signed figure** (not the legacy's two-gross-columns-unnetted quirk). **Ordering tie-break fixed**: `ORDER BY line_date, voucher_number, voucher_lines.id`. **Permission gate**: `any_segment_locked` implements the real rule ("no segment of the account filter is locked", checking only the segments actually given — matches `Is_Admin_Or_Valid_Daftar(_K,0,0,0)`'s own scoping, verified live that locking an unrelated Moein does NOT block a Kol-only query, only locking the Kol itself does) with a real `account_segment_locked` error, not the legacy's wrong "admin only" message; missing `accounts` rows are never locked (matches legacy). **Cross-fiscal-year**: `fiscalYearId` is `Option<i64>`, `None` = every period, no magic sentinel. **B5's other half, a scoped extension flagged not silently skipped** (same precedent as 4.5's A9 follow-up): `GET /api/v1/reports/party-balances`, the `BedBes`/R09 equivalent 6.2's own Build bullet names by name as needing the same `< from_date` fix — reuses `fetch_opening`/`fetch_movement` per party per control-account coordinate (3.1's `coordinate()`/`find_account_id_by_codes`, no fourth reimplementation of the windowing logic), debtor/creditor sign-flip and turnover (`GType`) filters ported, the legacy's default-amount-window "trap" (hides most parties below 1,000,000/above 100,000,000 out of the box) deliberately NOT reproduced (`ponytail:` O(parties×config-templates) queries per request, fine at this scale, documented upgrade path in the module doc comment if it ever needs to scale). New `api/tests/ledgers.rs` (4 `#[sqlx::test]`, live Postgres): B6 direct test (ledger shows data with zero journal generation ever run) + netted opening + same-voucher tie-break stability in one flow; B4/B5 direct test (coordinate view vs. `accountIds` view agree exactly, on-boundary date lands in the period not the opening); the lock-based permission gate rejecting a non-admin with the real error code; the party-balance list's boundary + debtor/creditor sign flip + amount-window filtering. Full suite `cargo test -p api`: 122/122 green (117 + 4 new integration; 1 new unit for the `signed()` helper already in that count). `cargo clippy -p api --all-targets`: clean, no new warnings. Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 (id 3) data: `GET /reports/ledger?generalLedgerCode=1` over the whole year returns `closingBalance: {amount:7700, side:"debit"}` — byte-identical to 6.1's independently-computed trial-balance `balanceDebit` for the same Kol, cross-checking both new report engines against each other on real data; the same query narrowed to `subsidiaryCode=11` (a specific Moein) returns the identical figure (single-Moein Kol in this dataset); missing `generalLedgerCode` and no `accountIds` returns 400; unauthenticated returns 401. `party-balances` returns an empty list against this tenant's real data (no `party_account_config`/postings coincide in this dataset) — a real, not simulated, empty-result path, not a failure. No `audit_log` rows expected or produced (read-only reports); no test data created against live data, only reused. |
| 6.3 | Card Jari statement | done | Genuinely small, per the doc's own framing: `GET /api/v1/parties/{id}/card-jari?fiscalYearId=` (new handler in `api/src/parties.rs`, next to 3.2's `get_party_balance`) delegates **entirely** to `party_balance::compute_balance` — the exact same function 3.2's own `/balance` endpoint calls, zero duplicated balance math, per the Build bullet's explicit "reusing 3.2's Jari_Rem-equivalent function directly." The only new logic is composing each breakdown row with a ready-made `ledgerHref` into 6.2's `/reports/ledger?accountIds=...` endpoint, scoped to that row's own `accountId` and the party's fiscal year's own `[start_date, end_date]` (04-04-a.md §4.1: Card Jari "has no date range at all... always inception-to-date for that year") — satisfies the Build bullet's "drill-through into the subsidiary ledger for any control account in the breakdown" without a caller ever having to reconstruct that query itself. **No new screen** — same documented Phase-6-has-no-UI-step gap as 6.1/6.2; Card Jari's own identity panel/photo (04-04-a.md §4.6) is explicitly out of scope too (party identity is already served by 3.4's existing party detail screen, no reimplementation needed there either). Extended `api/tests/party_balance.rs`'s existing worked-example test (no new test file — this genuinely is the same test, just carried one step further) to also call `/card-jari` and assert `total` matches the `/balance` endpoint's `-16,000,000` exactly (manual test #1), then follows one breakdown row's real `ledgerHref` through a live HTTP call and confirms it resolves to a correctly-scoped ledger showing that one account's own `-38,000,000`-equivalent (`38,000,000` debit) movement (manual test #2, drill-through actually lands somewhere correct, not just a well-formed URL). Full suite `cargo test -p api`: 122/122 green (no new test function — the existing worked-example test grew, so the count is unchanged from 6.2; the drill-through assertions are new coverage inside it). `cargo clippy -p api --all-targets`: clean, no new warnings. Verified live against the rebuilt `docker compose` containers via curl too, against a real fresh party/control-account/voucher built for this check (tenant `acme`, fiscal-year-1406 id 3): `/balance` and `/card-jari` both returned `total: -5,000,000` for the same party; the returned `ledgerHref` (`/reports/ledger?accountIds=177&fiscalYearId=3&fromDate=2027-03-21&toDate=2028-03-20`) was fetched directly and returned `closingBalance: {amount:5000000, side:"debit"}` matching the breakdown row's own `-5,000,000` remainder exactly — the "Done when" criterion (Card Jari and the Phase 3.2 API agree exactly) proven live, not just in the test suite. Test data (voucher, party, party_account_config row, 4 accounts) fully deleted afterward, confirmed via `psql`. |
| 6.4 | Stock / warehouse reports | done | New `api/src/inventory_reports.rs`: `GET /api/v1/reports/inventory-activity` **merges three legacy screens into one** — `Anbar_Amalkard` (subsystem A warehouse in/out) and `AnbarReportU` (subsystem B external-warehouse activity) turn out to have the *identical* three shapes (line detail / `(date,item)` subtotal / item grand-total) once the legacy's "one document type at a time" restriction is dropped to a plain optional filter; `AnbarReportKharid` (purchase/sales summary, unreachable in the legacy but named explicitly by the Build bullet for its B25 fix) is satisfied by the same endpoint's unconditional `fiscal_year_id` requirement. `groupBy` (`none`/`date`/`item`/`date_item`) picks one of four typed row shapes (`ActivityDetailRow`/`ActivityDateRow`/`ActivityItemRow`/`ActivityDateItemRow`) sharing one `ACTIVITY_PREDICATE` `&str` constant across all four SQL strings — B25's "fiscal-year predicate, applied uniformly" made structural, same discipline as 6.2's B4 fix. **B3 fix**: no statement in this module writes anything — direct contrast with `Anbar_Amalkard`'s three query builders each opening with an unconditional table-wide `UPDATE Anbar_FactorD SET AFD_Customer = (…)` (05-13-b.md §13.10's "critical defect"); `counterparty_account_id` (5.2's B7 fix) is already a real header-only column with nothing to repair, so the drift that statement existed to paper over cannot occur. **B16 fix**: no `CREATE`/`DROP TABLE` anywhere — every shape is one `SELECT`, no `RoyatJU`-style `temp_RJ_<uid>` materialisation. `GET /api/v1/reports/stock-balance` (Anbar_MandehU equivalent) reuses 5.3's canonical on-hand formula — **a real bug caught only by the new test, not by `cargo check`**: the first version LEFT-JOINed `inventory_document_lines` to `items` unconditionally and only scoped the *second* join to `inventory_documents` by fiscal year/date, so the `CASE WHEN d.document_type IN (...)` read `NULL` for out-of-scope rows and silently fell through to the ELSE (outbound) branch — every line for an item, from every fiscal year, was being subtracted regardless of scope; a genuinely B25-shaped defect this endpoint exists to prevent, introduced while building the fix for it. Rewritten as a correlated subquery (`STOCK_BALANCE_DIRECTION` constant reused, same predicate shape as `stock::compute_on_hand`, just correlated on `i.id` instead of bound to one item) so the fiscal-year/date/warehouse filter and the CASE live in the same scope — cannot silently fall through again. `lowStockOnly` reuses 5.3's own `isLowStock` comparison, not a literal port of the legacy's "negative balance" button. New `api/tests/inventory_reports.rs` (2 `#[sqlx::test]`, live Postgres): a full receipt→issue→cross-fiscal-year-leak-check→stock-balance-agreement flow (manual test #3/#4 — a second fiscal year's 9,999-unit receipt never leaks into the first year's activity report or its stock balance, and the stock balance's `70` exactly matches `100 - 30` from the activity report's own totals, proving no divergence between the two engines) — this is the test that caught the LEFT JOIN bug above, on its first run, before any live check; and a B3/B16 direct test (line/row counts in `inventory_document_lines` identical before and after running the report in all four `groupBy` shapes, and zero `temp_%`-named tables exist in `information_schema.tables` afterward). Full suite `cargo test -p api`: 124/124 green (122 + 2 new). `cargo clippy -p api --all-targets`: clean (one `#[allow(clippy::cmp_owned)]` on the same numeric(14,3)-vs-i64 low-stock comparison 5.3 already needed one for). Verified live against the rebuilt `docker compose` containers via curl too, against a real fresh warehouse/item/two-document fixture built for this check (tenant `acme`, fiscal-year-1406 id 3): `stock-balance` returned `onHand: "70"`; `inventory-activity?groupBy=item` returned `quantity: "130"` (100+30, the two documents summed regardless of direction) and `grossAmount: 130000`; `inventory-activity?groupBy=none` showed both lines individually with correct dates/types — cross-checked the on-hand figure (70 = 100 receipt − 30 issue) against the activity report's own raw numbers by hand, matching exactly. Invalid `groupBy` returned 400; unauthenticated returned 401. Test data (2 documents + lines, item, warehouse, unit, 7 accounts) fully deleted afterward, confirmed via `psql`. |
| 6.5 | Print pipeline | done | **Decision (user, 2026-08-23, via AskUserQuestion — RTL Persian PDF rendering had real technical uncertainty worth a real choice, not a guess)**: Typst, embedded in-process via `typst-as-lib` — pure Rust, no sidecar, real bidi + font shaping (`rustybuzz`), fits this codebase's single-binary deployment model. New crates: `typst`/`typst-pdf`/`typst-as-lib` 0.15/0.15/0.16, `derive_typst_intoval` 0.8, `thiserror` 2. **Environment note, not project-specific**: typst 0.15 requires rustc ≥1.92; this machine's toolchain was 1.91.1, upgraded via `rustup update stable` to 1.98.0 to unblock the build — anyone else building this repo locally needs the same. **A second environment finding, live-verified not guessed**: `cargo test -p api` (default parallelism) hits MSVC `LINK : fatal error LNK1102: out of memory` — Typst's dependency tree (373 crates) linked separately into ~15 integration-test binaries, several in parallel, exhausts the linker; `-j 2` (or lower) avoids it and is now this repo's required local test-run flag on Windows. Fonts: `Vazirmatn-{Regular,Bold}.ttf` (`api/assets/fonts/`, OFL-licensed, same family 1.6 already chose for the web frontend) fetched from the upstream project and `include_bytes!`'d into the binary — no runtime font install, no Docker image change needed. Templates: `api/templates/{voucher,tabular_report}.typ`, `include_str!`'d — **B23 closed structurally**, not just by omission: no route anywhere in this API accepts a template identifier or file (verified live in `pdf_routes_accept_no_template_selection_parameter`, a `?template=evil.typ` parameter is simply not read by any code path). New `api/src/pdf.rs`: **one structured `PrintHeader`** (organisation name — falls back to `tenants.name` since no step has ever seeded `organization`, same "no provisioning flow" gap already flagged for `account_code_format`/`party_account_config`/pistachio grades — fiscal-year caption, report title, period caption, amount-in-words, signature labels) consumed by both templates, replacing the legacy's 15+ string-literal memo names (04-06-a.md §6.1). Signature blocks read from `app_settings` (1.1's already-existing Tanzim equivalent, never populated by any step until now) via `fetch_signature_labels` — **fixes the legacy's "getter that writes the label as the value" self-healing bug structurally**: a missing key returns an empty string and is never inserted, so this can never reproduce "fresh installations print سند امضا ۴ where a signature block should be." New `api/src/persian_words.rs::amount_in_words` — a real function over `i64` (never `Dm.Str2String`'s string-sliced port, 04-06-a.md §6.4), scale words normalised to Persian yeh `ی` (deliberate deviation from legacy bytes, spec's own "otherwise normalise" allowance, since nothing requires byte-identical output against a legacy this rebuild isn't migrating from), scale ceiling at `تریلیون` (10¹²) with a documented plain-digit fallback beyond that. **Two routes wired, per a documented scope boundary**: `GET /vouchers/{id}/pdf` (also the print path for every cheque/deposit-slip/petty-cash document, since 4.2/4.4 already give them a real `voucher_id` — "print a cheque receipt" is "print its linked voucher," no bespoke template needed) and `GET /reports/trial-balance-4-column/pdf` (proves the tabular template + amount-in-words footer end to end, matching manual test #2). Ledgers/6-column-trial-balance/party-balance-list/invoices are the identical tabular shape (`tabular_report.typ` written generically for exactly this) but their own PDF routes are left for whoever next needs that specific download — same "generic engine ships, callers follow" precedent as 2.5's `auto_post`. **A real bug caught only by live curl, not by the test suite**: the trial-balance PDF's amount-in-words originally spelled out the grand total's *netted* balance, which is tautologically zero for any trial balance that actually balances (Σdebit=Σcredit nets to 0 by definition) — the live PDF said "صفر" (zero) where a real total belonged; fixed to spell out `proof.total_debit` (== `total_credit` when balanced), confirmed live afterward showing "هشت هزار و سیصد" (8,300) correctly. New tests: `pdf.rs`'s own `#[cfg(test)]` (3: `format_amount` grouping/zero-blanking, and two full compile-to-PDF-bytes checks proving the Typst pipeline actually works, not just that the Rust compiles) + `persian_words.rs` (5, incl. a zero-group-skipped case and a billion-scale worked example) + `api/tests/pdf.rs` (3 `#[sqlx::test]`, live Postgres, live HTTP: a posted voucher's PDF has the real `%PDF-` header and `application/pdf` content-type; the trial-balance PDF renders end to end through the real route; B23's direct test). Full suite `cargo test -p api -j 2`: 135/135 green (124 + 8 new pure unit + 3 new integration). `cargo clippy -p api --all-targets -j 2`: clean, no new warnings. **Verified live, visually, not just via magic bytes**: read the actual rendered PDF pages back (Claude's own PDF-reading capability) for both routes against the rebuilt `docker compose` containers and real tenant `acme`/fiscal-year-1406 data — the voucher PDF showed correctly-shaped Persian text (`فروش کالا`, proper lam-alef ligature, confirmed visually in the rendered page even though naive text-extraction reordered it, a known PDF-text-extraction bidi artifact unrelated to actual rendering), right-to-left table columns, and all 3 signature labels; the trial-balance PDF's `8,300`/`7,700`/`8,000`/`300` figures matched 6.1/6.2's independently-computed JSON figures exactly, cross-checking three separate reporting engines (trial balance, ledger, PDF) against the same real data. |
| 6.6 | Export pipeline | done | New `api/src/export.rs`: `write_csv`/`write_xlsx`, the two generic renderers every export in this domain goes through (`10-target-architecture.md` §3.4's own named choices — `csv` crate, `rust_xlsxwriter`, no judgment call needed unlike 6.5's PDF engine). **CSV**: real RFC 4180 quoting via `csv::Writer` — closes 04-07.md §7.1's exact defect ("`ForcedQuotes = False` with `Separator = ','` — a description field containing a comma breaks the row") structurally, not by convention. **XLSX**: bold header row, real numbers where a cell parses as one, no merges/repeated headers/spacer rows — the opposite of the legacy's `DataOnly = False` FastReport filters that "carry the page furniture." `collapse_whitespace` uses `str::split_whitespace` (collapses *any* run, not the legacy's single `'  '`→`' '` pass that "does not collapse runs of three or more spaces") and **no truncation at all** — the legacy's 200-character cut existed only to fit a `Space(100)`-preallocated Excel-COM cell, a constraint that doesn't exist for a real CSV/xlsx field, so the "cuts mid-word" hazard has nothing left to trigger it. **B17 fix**: `GET /api/v1/reports/tax-authority-export` (the `ToExcelDaraeiU` equivalent) filters `status = 'posted'` unconditionally — the legacy's export "had no `M_Tx` filter at all, so draft vouchers are exported to the tax authority," 04-07.md §7.2's own "most consequential instance of the missing state filter in the whole system." Same `kind = 'ledger'` filter every other report in this phase applies. **Column-header fix**: first column headed `شماره سند` ("voucher number"), not the legacy's `ردیف` ("row number") mislabelling a column that has always held `M_Sanad` — 04-07.md §7.2's own "a real defect in a file submitted to the tax authority." **B25 applied too**: `fiscal_year_id` required, no cross-year leak possible. Kol/Moein name resolution reuses the identical two-account-self-join pattern 6.2's ledger and 6.1's trial balance already use (ancestor-row natural-key lookup), not a fourth reimplementation. **No image/JPEG export built** — matches the Build bullet's explicit "not a real requirement once PDF (6.5) and clean data exports exist side by side; don't build the legacy's incidental raster-export capability." **Scope boundary, documented**: only the tax-authority export is wired to a real route this step (the one purpose-built, spec-detailed export in the legacy, `ToExcelDaraeiU`) — `MoeinZipU`'s "export current view" grid-to-Excel (04-07.md §7.3, no fixed column set, follows whatever the caller's grid shows) and per-report CSV/XLSX for the other 6.1-6.4 reports are left for whoever needs that specific download next, reusing `write_csv`/`write_xlsx` directly (both already generic over `&[&str]` columns + `&[Vec<String>]` rows, not tied to the tax export's own shape) — same "generic engine ships, callers follow" precedent as 2.5/6.5. New tests: `export.rs`'s own `#[cfg(test)]` (3: whitespace-collapse correctness, a real CSV round-trip proving a comma survives as one field, and an xlsx byte-signature check) + `api/tests/export.rs` (2 `#[sqlx::test]`, live Postgres, live HTTP): draft exclusion (B17) + correct column header + a real end-to-end CSV round-trip of a comma-and-multi-space Persian narration surviving as exactly one field (manual test #1-#3 in one flow), and a real `.xlsx` response with the correct content-type and zip-archive signature (manual test #4). **A real fixture bug caught by the test, not a code bug**: the first version of the test only created leaf accounts (10,1)/(40,1) without their own Kol-level ancestor rows (10,0,0,0)/(40,0,0,0) — the export query's ancestor-name lookup is an inner join (matching 6.1/6.2's own established convention), so it silently returned zero rows; fixed by seeding the ancestor rows, the same lesson 6.1's own test-debugging note already recorded once. Full suite `cargo test -p api -j 2`: 140/140 green (135 + 3 new unit + 2 new integration). `cargo clippy -p api --all-targets -j 2`: clean, no new warnings (one `useless_conversion` fixed before it shipped — `save_to_buffer()`'s own return type already matched the function signature). Verified live against the rebuilt `docker compose` containers via curl too, against real tenant `acme`/fiscal-year-1406 data: CSV export returned the correctly-headed, correctly-quoted real voucher lines (`Kol 1/Assets`, `Kol 4/Revenue`, matching every earlier phase-6 step's own live-verified figures); the `.xlsx` download was recognized by `file` as a genuine "Microsoft Excel 2007+" workbook (a real zip archive, not a stub); unauthenticated request returned 401. Scratch files cleaned up afterward. |
| 6.7 | Report permission gating | done | Wired every route built in 6.1-6.6 (previously plain `AuthUser`, the same "no dedicated Phase 6 wiring step until now" deferral 3.1/4.1/5.1 already used) to a real catalogue permission id, mirroring 2.8's own pattern for accounting core. New `authz::require`/`authz::require_any` (`api/src/auth/authz.rs`) — a runtime-check twin of `RequirePermission<P>` returning a JSON error body, needed because (like `vouchers.rs` since 2.8) the correct id sometimes depends on the request itself (the merged ledger route). **Six existing catalogue ids reused, mapped exactly**: `trial_balance_4_column`(1124, both the JSON and PDF routes), `card_summary`(1131, Card Jari), `warehouse_report`(1409, merges `Anbar_Amalkard`+`AnbarReportU` — both legacy screens already shared this one id), `inventory_balance_report`(1413, `Anbar_MandehU`), plus `view_subsidiary_ledger`(1123)/`general_ledger`(1141) both accepted on the merged `/reports/ledger` route (`require_any`) — DKolU and DMoein's two separate legacy ids, either one now unlocks the single endpoint that replaced both, same "any one of the ids a merged screen replaces" precedent `vouchers.rs`'s `list`/`get` already established. Voucher PDF upgraded from the view-only `LIST_VIEW` group to the catalogue's own dedicated **print** ids (1121 ledger / 1140 journal, `permcodes::print(kind)` added alongside `vouchers.rs`'s existing kind-dependent helpers) — a real B24-adjacent tightening: a PDF download is this rebuild's "print." **New `api/migrations/0019_report_permissions.sql` — the actual B24 fix, not just a wiring exercise**: three permission ids that never existed in the legacy catalogue at all (`trial_balance_6_column`, `debtors_creditors_report`, `tax_authority_export` — 04-01-a.md's own catalogue literally says "no permission key" for `Report5`/`Report8`/`EBC`), ids **3001-3003**, a clearly-marked "no legacy precedent" block (every other id in this catalogue is the legacy's own Pass_Config key, preserved for traceability; these three never had one to preserve, same honesty 2.3's migration already applied inventing id 0 for the missing 'manual' journal source). `Report4`(KolStateU)/`Report6`(voucher-number gap control)/`_Report9`(dead, `Visible=False`) — B24's other three ungated items — were never built by any Phase 6 step (genuinely out of the 6.1-6.6 Build bullets' scope), so nothing to gate; noted rather than silently ignored. New `api/tests/report_permissions.rs` (3 `#[sqlx::test]`, live Postgres): all 8 report routes 403 a fresh user with zero grants, including the three genuinely-new B24 ids (direct proof of the fix, not just of a check existing); each new id is independently grantable (granting one unlocks only that route, a second stays 403); the merged ledger route accepts either of its two legacy-precedent ids from two different single-permission users (manual test #3's "no repeat of the `Report1`/`Report2` split"). **A real regression caught by the existing suite, not a new bug**: `ledgers.rs`'s own `locked_segment_rejects_non_admin_with_the_real_rule` test (6.2) started failing once its route gained a permission check — its `plain` fixture user had zero grants at all, so the new 403 fired before the lock-specific check ever ran; fixed by granting that test's own user `general_ledger` first, isolating the lock behavior it actually tests (not a defect in the gating, a fixture that needed updating for the world it now runs in). Full suite `cargo test -p api -j 2`: 143/143 green (140 + 3 new). `cargo clippy -p api --all-targets -j 2`: clean, no new warnings. Verified live against the rebuilt `docker compose` containers via curl too: created a real non-superuser (`live67`), confirmed 403 on both `trial-balance-6-column` and `tax-authority-export` with zero grants, granted `trial_balance_6_column` directly via `psql`, confirmed it flips to 200 while `tax-authority-export` (still ungranted) stays 403 — the manual test's own "as a user without the permission... 403" and independence requirements, reproduced against the real running stack, not just the test suite. Test account disabled afterward (not force-deleted, to avoid violating the `audit_log` FK its own creation/login events are correctly attributed to — consistent with never mutating audit history for cleanup). **Phase 6 (Reporting) is now fully done** — all 7 steps (6.1 trial balances, 6.2 ledgers, 6.3 Card Jari, 6.4 stock/warehouse reports, 6.5 PDF print pipeline, 6.6 CSV/XLSX export, 6.7 permission gating), each self-verified live against the real running stack. |

## Phase 7 — Hardening & cutover
| Step | Title | Status | Notes |
|---|---|---|---|
| 7.1 | Constraint audit & enable | done | Full audit of every migration (0002-0019) against Group C2's checklist: accounts natural-key UNIQUE, voucher/invoice-number UNIQUEs, voucher balance CHECK, voucher-line debit-xor-credit + non-negativity, every FK (incl. `accounts.party_id`, `system_accounts.account_id`, `users.created_by/updated_by` self-FK, `fiscal_years.closing/opening_voucher_id`), no denormalised name/code columns — all already present and enforced from the phase that built them, confirmed by reading, not assumed. Two Group-C2 items deliberately NOT added, both pre-existing documented judgment calls this step didn't reopen: `accounts.parent_id`/`level` (2.1 chose positional segments instead, still C3-compliant) and cheque-number uniqueness (spec's own proposal requires a `banks` model, §13.13, that phase 4.1 already chose not to build in favour of free-text issuing-bank columns — a weaker self-invented substitute would misrepresent the spec's actual proposal, so left undone and documented in the new migration's header comment rather than faked). One genuine gap found and fixed: `02-11-g-ddl-inventory.md`'s "invoice total identity" (`total_amount = gross_amount + tax_amount − discount_amount`) was deliberately commented out in the legacy-migration DDL draft over truncation-compounding risk in historical data — this build has no historical data and computes the identity exactly by construction (`inventory_documents.rs`'s `LineAmounts`), so `api/migrations/0020_constraint_audit.sql` adds it as a real CHECK on both `inventory_documents` and `inventory_document_lines`. Reusable audit query set written to `docs/constraint-audit-queries.sql` (one violation-count query per constraint category, explicitly meant for 7.2's migration procedure) — ran it live, 0 violations everywhere. New `api/tests/inventory_documents.rs::total_identity_enforced_at_database_level` proves the CHECK rejects a raw INSERT that bypasses the API entirely (both header and line), matching the manual test's "confirm the database itself rejects it, not just the application layer." Full suite `cargo test -p api`: 144/144 green (was 143), clippy clean (0 new warnings — all 17 pre-existing). Verified live: rebuilt `docker compose` `api` container, migration applied cleanly, `/health` ok, both new constraints present in `pg_constraint`, audit query file run live against the real database via `psql` — 0 rows/0 counts on every check. |
| 7.2 | Migration readiness doc | done | `docs/migration-readiness.md`: copy-paste-ready SQL Server queries for every still-open Group A item (A1 Jalali decode incl. Nowruz-boundary probe, A9 `CheckMaster`/`CM_Count` distribution, A11 `Sahamdar_Edit` existence, A12 `S_IS_*` usage + `sys.jobs` search, A13 year-suffixed tables, A14 `Kinds` — direct question, no query can settle it, A15's 19 orphan procedures' `sys.jobs`/`dm_exec_procedure_stats` check, A16 `Sahamdar` DDL diff), each with a short note on what the answer changes in the already-built rebuild. §0 explicitly closes out A2 (DDL side)/A3-A8/A10/A17 so a future reader doesn't re-derive settled questions, per the manual test's #2. §9 restates the 6-step `10-target-architecture.md §4` migration procedure, cross-referencing which A-item blocks which step (table-discovery scope gated on A13/A14, ETL column mapping gated on A16, step 4 reuses 7.1's `docs/constraint-audit-queries.sql` verbatim) and reiterating step 6's reconciliation as the real acceptance test. Self-reviewed for the manual test's own two checks: grepped for placeholder/TODO markers (none) and confirmed every closed item is stated as closed rather than silently omitted. No code changed — a document-only step, nothing to build or run. |
| 7.3 | Backup/restore + new-fiscal-year wizard | done | **Open decision surfaced and resolved via AskUserQuestion (2026-08-23):** a whole-instance `pg_dump` necessarily contains every tenant's data (shared-DB tenancy, A3), but every existing admin gate (`RequireSuperuser`) is scoped to one tenant — reusing it would let any tenant's admin download every other tenant's data. User chose a new tenant-independent `users.is_platform_admin` flag (migration `0021_backups.sql`) + `RequirePlatformAdmin` extractor (`auth/authz.rs`), never implied by `is_superuser`. **Backup**: `api/src/backup.rs`'s `run_backup` shells real `pg_dump -Fc` (`tokio::process`) against the owner-level `DATABASE_URL` (the RLS-bound app pool can't see cross-tenant data, correctly, so can't dump it either) onto `BACKUP_DIR` (a new Docker named volume `backups_data`, mounted only into `api` — durable across recreation, never client-writable, the literal legacy defect §8.1 fixes), tracked in the new `backups` table (deliberately no RLS/tenant_id — instance-wide by construction). `POST/GET /api/v1/platform/backups`, `GET .../backups/{id}/download`, `PUT .../users/{id}/platform-admin` (grant/revoke, scoped to the caller's own tenant via RLS; the very first platform admin is a direct-DB bootstrap, same accepted gap as 1.1/2.1/3.1/5.1's other provisioning gaps). Retention (`BACKUP_RETENTION_COUNT`, default 10) prunes both row and file after every successful run. Scheduled backup is a real `tokio::spawn` interval task (`BACKUP_INTERVAL_SECS`, default 86400s) wired in `main.rs`, never inside a request handler — fixes the legacy's "auto-backup ran unconditionally from every login" defect (§8.1). **Restore is deliberately NOT an HTTP endpoint** (documented in `backup.rs`'s module doc comment) — restoring over the live shared multi-tenant DB while running would nuke every tenant's current data from one snapshot; `scripts/restore-backup.sh` is the real, tested path instead (ops-only, `pg_restore` into a target DB, scratch by default). **New fiscal-year wizard: discovered already fully satisfied by 1.5/1.6** — reachable via a normal `RequireSuperuser`-gated action (not a hidden gesture, B20's own fix), validates date order + uniqueness via existing 409 mapping, never copies the prior year (no copy logic ever existed), chart of accounts stays global (A6) — no new work needed, re-verified rather than re-built. **Incidental fix required to rebuild at all**: `cargo-chef:latest-rust-1` (the API Dockerfile's builder base) had drifted from Debian bookworm to trixie upstream since Phase 0.1, so the compiled binary (glibc 2.39) no longer ran on the pinned `debian:bookworm-slim` runtime (glibc 2.36) — runtime bumped to `debian:trixie-slim` to match, unrelated to this step but blocking every rebuild until fixed; documented in the Dockerfile itself. Runtime image also gained `postgresql-client-17` via the PGDG apt repo (version-matched to the `postgres:17` service, confirmed live: both report 17.11). New `api/tests/backup.rs` (5 `#[sqlx::test]`): tenant-superuser-is-not-platform-admin (the core of this step), plain-user/anonymous rejected, platform-admin can list, grant/revoke round-trip (plain user can't grant), download guards against running/nonexistent backups — deliberately does NOT exercise real `pg_dump` (not installed on this Windows dev host; `RequirePlatformAdmin` rejects before the handler body runs regardless, so the permission tests are meaningful without it). Full suite `cargo test -p api`: 149/149 green (was 144); clippy clean, 0 new warnings (fixed one very-complex-type warning of my own with a type alias). **Verified fully live** against the rebuilt stack: bootstrapped `root` as platform admin (direct DB, the documented gap), triggered a real backup via curl (224KB `.dump` landed on the volume), listed it, downloaded it (byte-exact), then **actually restored it into a scratch Postgres database via `pg_restore`** and diffed row counts across 5 tables against the live source — exact match (tenants/users/vouchers/accounts/backups all equal) — the real proof this step's Build bullet asks for, not backups taken on faith. Retention verified live too: set `BACKUP_RETENTION_COUNT=2`, triggered a 5th backup, confirmed exactly 2 rows and 2 files survived. Every permission gate re-confirmed live (platform admin 200, tenant superuser 403, plain user 403, anonymous 401). Test data (manual sessions, backup rows/files) cleaned after; `root.is_platform_admin=true` left in place as legitimate bootstrap state, not test residue. |
| 7.4 | Production Docker/deploy | done | **`docker-compose.prod.yml`** (override, not a template layer): `db` loses its published host port (only `api` needs it — local dev's base file still publishes it for `psql`/`cargo test` against `localhost`), `api` gets `RUN_MIGRATIONS_ON_BOOT=0` (explicit-step migrations in prod, matching `main.rs`'s own long-standing comment) + `SESSION_COOKIE_SECURE=true`, both `api`/`web`/`db` get memory `deploy.resources` limits. TLS termination deliberately left as an external infra concern (documented in the file's own header), not a bundled nginx container — `web` already runs as a real Next.js Node server (Server Components/Actions, per 1.6), so neither Build-bullet option ("static bundle behind nginx" / "served by the API") needed new work, just documentation of which applies. **Real bug found and fixed**: `web`'s runtime image was 936MB — the Dockerfile copied the FULL `node_modules` (devDependencies included: eslint, `@types/*`, Tailwind build tooling) into the runtime stage instead of using Next's own dependency-tracing output. Fixed with `output: "standalone"` (`next.config.ts`) + rewriting `web/Dockerfile`'s runtime stage to copy `.next/standalone` + `.next/static` + `public` and run `node server.js` instead of `npm start` — 936MB → 264MB (3.5×), confirmed the rewrite-to-API proxy still works post-switch (baked into `.next` at build time, unaffected by the runtime-stage change). `docs/production-deploy.md`: build/run commands, the explicit-migration-step procedure (honestly incomplete — no CI/CD platform chosen yet to run a clean `sqlx migrate run`, documented as the honest current state rather than invented), and this step's own 4-point manual test procedure. **All 4 manual-test points executed live, not just described**: (1) built both prod images, checked sizes — `api` 313MB (trixie-slim + postgresql-client-17, no cargo/rustc baked in), `web` 264MB post-fix, both reasonable; (2) ran the actual `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d` with the real stack — confirmed `db`'s port genuinely unpublished (`docker compose port db 5432` → nothing) and all three services healthy; (3) `docker compose stop db` → `/health` correctly reported `db: unreachable` while `api` stayed up (no crash) → `docker compose start db` → `/health` recovered to `db: ok` within its own healthcheck interval with **no `api` restart** (bare `sqlx::PgPool` reconnect, nothing bespoke needed); (4) `grep -nE "password[:=]['\"][^$\"']"` across every Dockerfile/compose file → zero matches, only environment-variable references, confirming Phase 1's credential handling never regressed into a file-based secret. Frontend re-verified after the Dockerfile/next.config.ts changes: `next build` (Turbopack, bundled TS checker) compiles clean, all 22 routes generate, `eslint .` clean — a stray `npx tsc --noEmit` run against a mismatched global TypeScript version threw 2 environmental errors ("bundler" moduleResolution unrecognized) that `next build`'s own bundled checker doesn't share; not a real regression, noted and moved on rather than chased. **Incidental infra note**: mid-step, Docker Desktop on this dev machine stopped responding and needed a manual restart+cold-boot wait (WSL2 backend) before rebuilds could resume — a local machine hiccup, not a project defect, not otherwise recorded. Cleaned up: reverted to the base (non-prod-override) compose stack afterward so local dev's published `db` port and auto-migrations keep working for the next step. **2026-08-23 follow-up (post-Phase-7 close, user-requested):** the two items left as external/honest-gaps above were both closed on explicit user choice (AskUserQuestion: GitHub Actions for migrations, Caddy for TLS) — `.github/workflows/migrate.yml` (`workflow_dispatch`-only, installs `sqlx-cli`, runs `sqlx migrate run` against a `PROD_DATABASE_URL` secret — deliberately manual-trigger, not on-push, since a prod schema migration warrants a deliberate click) and a real `caddy` service in `docker-compose.prod.yml` (repo-root `Caddyfile`, automatic Let's Encrypt via `SITE_DOMAIN` or a self-signed internal-CA cert when unset) fronting `web` on 80/443; `web`'s host port now unpublished in prod too (only `caddy` is public ingress, same pattern as `db`). Verified live: YAML syntax validated (`python:3-alpine` + `pyyaml`, since no local Python/js-yaml), full merged `docker compose config` clean, and Caddy itself brought up and curl-tested end-to-end (self-signed cert issued for `localhost`, HTTP→HTTPS 308 redirect, HTTPS→`/login` 200 through the full proxy chain to `web`) — via a temporary alternate port mapping since 443 was already OS-reserved on this dev host (PID 4, a Windows-reserved range unrelated to this project); the committed `Caddyfile`/compose service itself is unchanged and uses the real 80/443. `.env.example` gained `SITE_DOMAIN`. Reverted to the base dev stack afterward. |
| 7.5 | Reconciliation test harness | done | New `api/tests/reconciliation.rs` — the dedicated, purpose-named harness the Build bullet asks for, consolidating (not duplicating the plumbing of) every worked-arithmetic example the plan cites: party balance (3.2, `07-06-a.md` §6.3 — via the real `/parties/{id}/balance` endpoint, `-21,000,000`), average purchase cost (5.4, `05-06-a.md` §6.2.1 — via the real `/items/{id}/average-cost` endpoint, `59,000`), and pistachio deduction Examples A & B (5.6, `05-08-a.md` §8.2.3 — calling `pistachio::compute_deduction` directly, now `pub` for exactly this, since it's the pure formula itself and the point of a reconciliation harness per `10-target-architecture.md §6` is testing "the rules that must not drift," not re-exercising the HTTP/auth stack each phase's own test file already covers). Example C (rounding-mode divergence) deliberately left where it already lived (`money.rs`, a pure rounding identity, not a report-level reconciliation) rather than duplicated. Harness's own doc comment states the extension path for 7.2's eventual real legacy-data migration: one more function in the same "seed known inputs → call the real production code path → assert an exact reference value" shape, no restructuring. **All 3 manual-test points executed live**: (1) fresh run, 4/4 pass against the current system; (2) deliberately broke `compute_deduction`'s moisture-percentage formula (÷99 instead of ÷100) → both pistachio cases failed immediately with exact, readable before/after diffs (party-balance and average-cost cases correctly kept passing, proving the harness isolates the actual broken rule rather than failing everything indiscriminately); (3) reverted → 4/4 green again. Full suite `cargo test -p api`: 153/153 (was 149); clippy clean, 0 new warnings. Docker `api` container rebuilt clean to confirm the `pub` visibility change compiles in the real image too (test-only file itself doesn't ship, but the source change does); `/health` ok post-rebuild. **This closes Phase 7 and the full 46-step roadmap** — every step in `docs/01-roadmap.md` is now `done`. |
